# CLAUDE.md - Memory & Project Documentation

## 🏷️ Project Overview
**PROFAS Leadership LMS** - Executive Leadership Academy & Learning Management System.
- **Type**: SaaS Learning Management System (LMS) / Interactive E-Learning Platform.
- **Tech Stack**: Next.js 15 (App Router), React 19, TypeScript, Tailwind CSS / Vanilla CSS, Prisma ORM, NextAuth.js / Google OAuth, Vercel Serverless.
- **Current Status**: Live on Vercel Production (`https://profas-leadership-lms.vercel.app`) with dynamic Google OAuth callback and serverless SQLite auto-copying to `/tmp`.

---

## 🛠️ Essential Commands
- **Local Development**: `npm run dev` (Runs on port 3000)
- **Build**: `npm run build` (Executes Prisma generate, schema push, seeding, and Next.js production build)
- **Typecheck**: `npm run typecheck`
- **Lint**: `npm run lint` (auto-fix mechanical issues: `npm run lint:fix`)
- **Duplicate-code check**: `npm run dup:check` (jscpd, config in `.jscpd.json`)
- **Smoke Test**: `npm run smoke`
- **Autopilot Loop**: `npm run autopilot:start` / `npm run autopilot:status`

---

## 🎨 Architectural & Style Guidelines
1. **Frontend Design**:
   - Prioritize modern **Glassmorphism** aesthetics (`backdrop-blur-md`, subtle semi-transparent borders, glowing gradients).
   - Dynamic dark/light mode compatibility.
   - Smooth micro-animations and responsive layouts.
2. **Backend & Database**:
   - Prisma ORM managing relational entities (User, Course, CourseNode, Assessment, Certificate, Enrollment).
   - Serverless compatibility: SQLite database (`dev.db`) is automatically copied to `/tmp/dev.db` in serverless environments (Vercel/AWS Lambda) to avoid read-only filesystem errors.
   - Prepared for future migration to Cloud PostgreSQL (Neon/Supabase).
3. **Coding Standards**:
   - Modular, clean TypeScript code with strict typing.
   - Avoid ad-hoc utility clutter; leverage reusable design tokens and components.
   - Preserve existing documentation and docstrings.
4. **Folder Structure** (adapted from standard React layout to Next.js App Router — `app/` stays at root for routing):
   - `app/` — routes only (App Router convention, cannot move).
   - `components/ui/` — small, reusable presentational components (Header, Footer, Logo, cards, chrome).
   - `components/shared/` — feature/business-logic components (CoursePlayer, Quiz, admin/mentor panels, export buttons).
   - `services/` — DB client, auth, rate-limit, uploads, and `services/export/` (docx/pdf/pptx/xlsx generators).
   - `utils/` — small pure helper functions.
   - `hooks/` — reusable client-side React hooks (e.g. `useLocalBackup`).
   - `context/` — reserved for React Context providers (empty until a real cross-cutting client state need appears).
   - `types/` — shared TypeScript types not already covered by Prisma-generated types.
   - `constants/` — reserved for cross-cutting constants (domain enums live in `prisma/schema.prisma` instead).
   - `styles/` — global CSS, imported from `app/layout.tsx`.

---

## 🔄 Self-Improvement & Progress Log
- **2026-07-26 [Phase 57 - Course Player LINK-Lesson Type: Retire The Off-Palette Purple Theme]**: Forty-seventh autonomous cycle, taking item 3 from the Phase 56 audit backlog. The course player's "TAUTAN SUMBER DAYA EKSTERNAL" (LINK lesson type) screen carried three coordinated violet elements — `.player-link-circle` (icon chip, `#f3e8ff`/`#7e22ce`), `.player-btn-external` (the "Buka Tautan Eksternal" CTA, `#7e22ce` with a violet-tinted shadow and `#6b21a8` hover), and an `eyebrow-teal player-eyebrow-purple` label — none of which belong in the royal-blue palette.
  - **Measured before assuming all three were live bugs, and one wasn't.** `.eyebrow-teal` (the base class already on the label) sets `color: #1e5a8f !important` — and since both it and `.player-eyebrow-purple` live in the same `@layer legacy` block, `!important` wins over the non-important override regardless of source order or specificity. A live-rendered check on `main` confirmed the label's computed colour was already `rgb(30, 90, 143)` (`#1e5a8f`, on-palette) — **`.player-eyebrow-purple`'s `color: #7e22ce` had never once rendered**. The icon circle and button, which have no competing rule, measured genuinely off-palette: `rgb(243, 232, 255)`/`rgb(126, 34, 206)` background/icon, `rgb(126, 34, 206)` button background.
  - **Deleted the dead rule instead of renaming it.** The initial instinct was to rename `.player-eyebrow-purple` to something colour-neutral now that its value no longer matched its name — but since the rule can never win against `.eyebrow-teal`'s `!important` regardless of what colour it holds, a renamed version would still be exactly as dead as the original. Removed the CSS rule and the class from the JSX instead; verified the label's rendered colour is byte-identical before and after (`rgb(30, 90, 143)`, unchanged).
  - **Fixed the two rules that do render**: `.player-link-circle` → `background: var(--teal-light)` / `color: var(--teal)` (the same light-chip convention as `.analytics-icon-blue` elsewhere in the file); `.player-btn-external` → `background: var(--teal)`, hover `var(--teal-dark)`, shadow `rgba(42, 107, 167, 0.3)` — reusing tokens already defined and used throughout `design-system.css`, no new colour introduced.
  - **Verified live**: icon chip `rgb(239, 246, 255)`/`rgb(42, 107, 167)`, button `rgb(42, 107, 167)` → hover `rgb(30, 90, 143)`, shadow `rgba(42, 107, 167, 0.3)`, all measured via a temporary probe rendering the exact JSX/CSS combination (the real page needs a live DB). Screenshot confirms a cohesive blue treatment with no residual purple.
  - **Verified**: typecheck (exit 0), changed-file lint, production build (54/54 routes), perf budget (794/793/853 KB, unchanged).
  - **Backlog remaining from the Phase 56 audit**: off-palette inline gradients in `app/dashboard/page.tsx` (needs touching the component, not just CSS); `.pro-expert-badge` forum mentor/facilitator badge (violet/yellow, inconsistent with the amber `.forum-mentor-badge` used for the identical concept elsewhere); `.question-card` on `/kuis` and `/evaluasi` swept into a dashboard-wide "force opaque white" `!important` rule that kills its own glass background.
- **2026-07-26 [Phase 56 - Quiz Result Screen: Fix Clipped Confetti, Off-Palette CTA, And A Missed Teal Literal]**: Forty-sixth autonomous cycle, and the first sourced from a 10-agent parallel audit workflow (6 independent auditors → synthesis ranking → 3-way adversarial verification) rather than a solo sweep. The workflow's own verification step **refuted its own top-ranked finding** before any code was touched — recorded below because that catch is as valuable as the fixes that shipped.
  - **Rank 1, refuted**: a dashboard stat-card color override (`styles/lms-fresh.css:334-336`) was flagged as "defeating" a per-card gradient the component computed, with two off-palette hues. The verifier read three lines *above* the disputed ones and found an unconditional rule (`:326-333`) already forces every `.stat-card-icon` to the same royal-blue/mint pair regardless of nth-child — so the inline `gradient` prop was already fully dead code independent of the flagged lines, and deleting them would make the dashboard *more* uniformly blue, not "restore" per-stat color as claimed. It also found the two "new" hues are the exact literals already used by name-carrying classes (`.analytics-icon-amber`, `.analytics-icon-blue`) in the same file — an existing rotating-accent pattern, not an accidental violation. **Not implemented**, and the underlying inline gradients in `app/dashboard/page.tsx` (many themselves off-palette: `#6d28d9`, `#8b5cf6`, `#f59e0b`…) remain a genuine open item for a future cycle that can afford to also touch the component, not just the CSS.
  - **Rank 2, confirmed and shipped**: `.player-pulse-circle` and `.player-btn-quiz` (the "Mulai Mengerjakan Kuis/Tugas" CTA in the course player) used Tailwind cyan (`#06b6d4`, `#0891b2`) unlike everywhere else in the file. **Resolved a real ambiguity before implementing**: the suggested replacement was `#38bdf8`, a hex this project has previously called off-palette and removed from the hero, command palette, and shimmer trophy. Checked directly rather than trusting either side — `#38bdf8` recurs **6×** in `design-system.css` as an established light-blue gradient stop paired with the primary blue (`#1e5a8f → #2a6ba7 → #38bdf8`, `.testi-avatar-icon.bg-cyan`), which is a different, deliberate in-file pattern from the standalone flat-color mismatches removed in those earlier, unrelated files. Swapped `#06b6d4 → #38bdf8` and `#0891b2 → #0284c7`, both already established literals in the same file.
  - **Rank 3, confirmed and shipped**: the quiz pass-celebration confetti (`components/shared/Quiz.tsx:69`) was visually clipped. `.quiz-result` carried inline `overflow: 'hidden'` and the class `pro-celebrate-check`, whose `scale-bounce` keyframe ends on a non-`none` transform with `fill-mode: forwards` — per spec that makes the wrapper a containing block for its `position: fixed` confetti descendants for the rest of the celebration, so they rendered clipped inside the card instead of spilling across the page as authored. Removed the inline `overflow: 'hidden'` (the card's rounded corners come from its own `borderRadius`, not from clipping children).
  - **Also fixed, found by me while verifying Rank 3 in the same file**: two inline `rgba(13, 148, 136, *)` literals on the same result screen (`boxShadow` on the pass/fail badge, `background`/`border` on the correct-answer stat panel) — the old off-palette teal purged from every `.css` file across Phases 19/47/48, but never caught there because that sweep only grepped stylesheets, not inline JSX styles. Converted to `rgba(42, 107, 167, *)` and the panel background to `rgba(239, 246, 255, *)`, matching the established `--al-primary-light` token.
  - **Declined, consistent with prior precedent**: the workflow separately flagged the confetti particle color array (`#2a6ba7, #0284c7, #f3b444, #10b981, #ef4444, #8b5cf6, #ec4899, #38bdf8`) as off-palette. Phase 34 already investigated the identical pattern in `CompletionCelebration.tsx`'s confetti and explicitly ruled multi-colour celebration confetti a deliberate art-direction choice, not a bug — the same reasoning applies here, so this was **not** touched.
  - **Verified the confetti fix visually, not just structurally**: `getComputedStyle(...).overflow` measured `"hidden"` on `main` and `"visible"` on this branch (isolated by `git stash`), and a mid-animation screenshot (350 ms after submit) shows confetti particles genuinely spilling past the card's rounded top edge into the surrounding page — the exact failure mode described. The CTA gradient fix was isolated in a standalone probe and measured as `rgb(42,107,167)→rgb(56,189,248)` and `rgb(42,107,167)→rgb(2,132,199)`, both cohesive royal-blue, white text retaining full contrast.
  - **Verified end-to-end via a temporary probe route** that mounted the real `Quiz` component with a stubbed `/api/assessments/submit` response (the page needs a live DB), submitted a real answer, and read the actual rendered result screen — not a hand-rolled replica of the markup.
  - **Verified**: typecheck (exit 0), changed-file lint, production build (54/54 routes), perf budget (794/793/853 KB — unchanged).
- **2026-07-26 [Phase 55 - Remove The Fabricated "Live Activity" Feed From The Landing Page]**: Forty-fifth autonomous cycle. Deleted `LiveActivityFeed`, which showed every landing-page visitor a rotating toast built entirely from a hardcoded `DUMMY_ACTIVITIES` array — five **invented named individuals** with fabricated recent timestamps (*"Budi P. baru saja menyelesaikan modul Komunikasi Asertif — Baru saja"*, *"Sarah A. mendapatkan sertifikat … 2 menit lalu"*). Nothing connected it to real data and nothing labelled it as illustrative.
  - **This is the same bug class Phases 12–17 systematically removed** (fake student counts, fake login streaks, hardcoded "Aktif" statuses), and it directly contradicts `soul.md:22` — *"Hindari data atau kemampuan palsu."* It was the most public instance and the only one that invented named people doing specific things minutes ago.
  - **Confirmed it genuinely rendered before acting**, rather than assuming a dormant component: sampled the landing page over time and watched it go `opacity: 0` → **`opacity: 1`, on-screen at ~3.5 s**, hold ~5 s, then cycle indefinitely.
  - **Flagged rather than deleted unilaterally, then deleted on the owner's instruction.** Removing a visible element from a marketing page is a product decision, not a defect fix — `soul.md` also permits *"konten demo boleh representatif"* — so the finding was presented with three options (remove / wire to real `ActivityLog` data / label it as illustrative) and the owner chose removal.
  - **Deliberately did not touch the static claims** — "2.500+ alumni", "4.9/5" — which are ordinary marketing copy for the owner to stand behind. The activity feed differed because it simulated real-time events attributed to named people.
  - **Removed cleanly**: the import and render site in `app/page.tsx`, plus `components/ui/LiveActivityFeed.tsx` and its co-located `LiveActivityFeed.css`. Zero residual references to `LiveActivityFeed`, `al-live-feed` or `al-feed-icon` anywhere in `app/`, `components/` or `styles/`. This also retires the `.al-live-feed-close` tap-target item left open in Phase 54.
  - **Verified**: `.al-live-feed` absent and the string "Budi P." absent from the DOM after a 7 s wait (well past the old 3 s appear + 5 s cycle); hero still Fraunces at 357px; footer social chips still 2; `docOverflow` 0; zero console errors; typecheck (exit 0), changed-file lint, production build (54/54 routes), perf budget — landing **799 → 794 KB**.
- **2026-07-26 [Phase 54 - Mobile Tap Targets; A Dead Footer Rule Restored]**: Forty-fourth autonomous cycle, and the first from a **fresh audit** — both prior backlogs (the 9-lens roadmap and the Phase 20 design/animation list) are fully closed.
  - **Audited responsiveness across all seven public routes at 320px and 390px**, checking horizontal overflow, elements escaping the viewport, and tap-target size. **Horizontal overflow was 0px everywhere** — the Phase 38 sticky-header/overflow work holds. The consistent failure was **tap targets below the WCAG 2.5.8 (AA) 24×24 minimum**.
  - **The worst one is the mobile navigation itself.** `.menu-button` — the hamburger, which is the *only* navigation on a phone — measured **21×44px** on every public route. Also failing: the password show/hide toggle at **18×44** on `/masuk` and `/daftar`, and the footer's Instagram/LinkedIn links at **18×18**.
  - **The footer one turned out to be a dead rule, not just an a11y gap.** `master.css` authors `.socials span { width: 32px; height: 32px; border-radius: 8px; background: rgba(255,255,255,.07) }` — but `Footer.tsx` renders `<a>`, and `.socials` appears in exactly one file with **zero `<span>` children**. The rule matched nothing, so the links collapsed to their 18px icon. Retargeting it to `.socials a` restores the intended chip **and** fixes the tap target in one change.
  - **Preserved the visual position rather than just enlarging the boxes** (the Phase 23 pattern): padding plus a compensating negative margin. Verified against `main` — the hamburger icon's centre is **byte-identical at 342,33**.
  - **Caught my own 4px regression mid-cycle.** The first password-toggle fix used symmetric padding with a shifted `top`, which moved the icon **4px up** — because the button has a fixed 44px height, so the icon centres within it and vertical padding does nothing while the changed `top` moves the whole box. Switching to horizontal-only padding restored the icon to exactly **336,666**, matching `main`.
  - **Three findings deliberately left alone, each for a stated reason**: inline text links (190×22, 127×20 …) fall under WCAG 2.5.8's **inline exception**; the 14px "Ingat saya" checkbox sits inside a `<label>` that provides a far larger effective target; and `.al-live-feed-close` is *authored* at 24×24 in `components/ui/LiveActivityFeed.css` yet measures **22×40**, so something overrides it — that needs its own investigation rather than a guess.
  - **Two false leads discarded before acting**, both of which would have produced bogus "fixes": (1) `.player-main` carries `margin-left: 326px !important`, which looked like it would break the course player on mobile — but `enterprise-lms.css` **and** `lms-fresh.css` both reset it inside `@media (max-width: 1000px)`, matching `master.css`'s breakpoint exactly. (2) `LiveActivityFeed` appeared to ship with **zero CSS** — until a wider search found a co-located `components/ui/LiveActivityFeed.css`; the earlier grep had only covered `styles/`.
  - **Verified**: sizes before/after (hamburger 21×44 → **33×44**, password toggle 18×44 → **26×44**, socials 18×18 → **32×32**); icon centres compared against `main`; full seven-route sweep re-run at both widths with 0px overflow; typecheck (exit 0), production build (54/54 routes), perf budget (799/793/853 KB).
- **2026-07-26 [Phase 53 - The Certificate Name Was Rendering At Weight 900]**: Forty-third autonomous cycle, taking the font-weight defect Phase 52 measured and deferred. Fixed one, and **declined the other on its own measurement**.
  - **The defect**: `styles/master.css` authors `.certificate-paper h2 { font-weight: 700 }`, but `app/layout.tsx` loaded Fraunces at only **400/600/900**. With no 700 face, CSS font matching resolves upward, so **the recipient's name on every certificate rendered at 900** — visibly heavier than authored, on the single most prominent element of a formal document. This is the same `h2` that Phase 42 pointed at Fraunces in the first place.
  - **Proved it from the font system, not from the stylesheet.** `getComputedStyle` reports the *authored* weight regardless of which face is used, so it cannot detect this. Enumerating `document.fonts` did: the loaded set was `Fraunces 400/600/900` with **no 700**. Rendered width then confirmed the fix — the name measured **473.69px → 461.25px**, narrower, consistent with dropping from 900 to a real 700.
  - **Weights now track what the CSS authors**: `['400','600','700']`. 900 is authored nowhere in the codebase and was only ever reached as that fallback, so dropping it leaves the file count unchanged (16 files, 464 KB) — a correctness fix at zero cost.
  - **Declined the second half after measuring it.** Space Grotesk is authored at 600 by `.al-float-card--ledger small` and resolves up to 700. Adding a 600 face was implemented, measured, and then **reverted**: the caption rendered **115.77px vs 115.75px** — a 0.02px difference for a whole extra font file. The intent was real but the payoff is not, and a comment in `layout.tsx` now records that so a later cycle does not "fix" it again.
  - **Verified**: `document.fonts` before/after; certificate name width before/after; landing page unchanged (4 files / 149 KB, hero and FAQ still Fraunces 600, zero console errors); typecheck (exit 0), changed-file lint, production build (54/54 routes), perf budget (799/793/853 KB).
- **2026-07-26 [Phase 52 - Stop Preloading Display Fonts On Routes That Never Use Them]**: Forty-second autonomous cycle. Cut **102 KB (−68%) of font download off every route except the landing page**, by measuring what a browser actually transfers instead of trusting build-manifest arithmetic.
  - **Changed measurement method, and that is what found it.** Every prior perf cycle summed `.next/app-build-manifest.json`. This cycle instead drove a real browser and recorded response sizes per route. Result: `/`, `/masuk` and `/program` each pulled **149 KB of fonts — identical on all three**. Then a per-file breakdown showed `/masuk` renders **only Inter** yet downloaded **all four** font files. Next.js preloads every font declared in the root layout; because Fraunces and Space Grotesk are applied through CSS variables in global stylesheets, Next cannot tell which route uses them.
  - **Fix**: `preload: false` on Fraunces and Space Grotesk only. Inter is the body face on every page and keeps its preload. Measured after: `/masuk` **149 KB → 47 KB, 4 files → 1**; `/` still loads all three families (it genuinely uses them) but three of four are now fetched on demand rather than preloaded. Production build confirms **1 preloaded font file instead of 4**.
  - **Checked the actual risk rather than waving it through.** Removing preload from a display face can cause a flash of fallback text on the hero — the most prominent element in the product. Sampled the hero's computed `font-family` every 100 ms from first paint on a throttled connection (1.5 Mbps, 150 ms latency): the timeline shows **`Fraunces` with no intermediate fallback state**. `display: 'swap'` was already in place, so the swap behaviour itself is unchanged.
  - **Two font-config defects found and deliberately NOT changed** (they alter rendering, so they belong in their own cycle): the CSS authors `font-weight: 700` with Fraunces, but only 400/600/900 are loaded, so the browser resolves 700 → **900**; and Space Grotesk is authored at **600**, which is not among its loaded 400/500/700. Aligning declared weights with authored ones is a correctness fix with visible weight changes, not a size win.
  - **Verified**: typecheck (exit 0), changed-file lint, production build (54/54 routes), perf budget (799/793/853 KB), per-route font transfer measured before and after.
  - **Method note**: `perf:budget` and the build manifest measure *bundle* weight; they do not see fonts, and they cannot tell preloaded from on-demand. For "what does a user actually download", drive a browser and read response sizes.
- **2026-07-26 [Phase 51 - Finish The Lazy-Export Sweep; Fix A Crashing DOCX Button Found While Verifying]**: Forty-first autonomous cycle, completing the pattern Phase 50 established and repairing a genuine defect that only surfaced because the change was verified end-to-end instead of by inspection.
  - **`/sertifikat/[number]` was the heaviest route left at 766 KB, and it is a *public* page.** Certificate links are shareable and verifiable, so anyone opening one downloaded a 322 KB chunk of jspdf + html2canvas before clicking anything. Cause: `CertificatePDFButton.tsx:5` statically imported `@/services/export/pdfGenerator`. Moving it into the click handler took the route to **371 KB (−395 KB, −52%)**.
  - **Swept for the rest of the pattern rather than fixing one instance**: `ExecutiveExportHubModal` statically imported **all four** generators. It is already behind `next/dynamic({ssr:false})` so it never touched the dashboard's initial load — but opening the modal pulled xlsx + pdf + pptx + docx just to produce whichever single file the user picked. Each generator now loads inside its own handler. Two handlers (`handleExportPdf`, `handleExportPptx`) had to become `async`, which typecheck caught immediately.
  - **The verification found a pre-existing crash.** With the modal's data endpoint stubbed so its buttons could actually be exercised, three exports produced files and **the DOCX one threw** `Cannot read properties of undefined (reading 'map')` inside `docxExport.ts`. Confirmed on `main` before touching it: the modal mapped modules to `{title, type, durationMin}` while `generateSyllabusDocx` expects `SyllabusModule` = `{title, duration, description, keyTakeaways}`, so it crashed on `mod.keyTakeaways.map` for **any course that actually had modules**. **TypeScript could not catch it** because `course` comes from an untyped `fetch`, so `any` propagated through the `.map()` and made the wrong shape assignable. Fixed the mapping; "Unduh Silabus Word (.docx)" now works.
  - **Shipped the fix rather than logging it**, because leaving a button I had just proven crashes, inside the very component being changed, would have been indefensible.
  - **Verified end-to-end with real downloads**, since this is runtime behaviour: certificate PDF 7,155 bytes; and from the hub — xlsx 8,499, transcript PDF 10,353, slide deck 15,628, **docx 9,695** (previously a crash). Zero console errors.
  - **Verified**: typecheck (exit 0), changed-file lint, production build (54/54 routes), perf budget (799/793/853 KB).
  - **Measured, not yet acted on**: `/layout` is **741 KB on every route — 361 KB JS + 380 KB CSS**, the largest remaining lever in the product. Splitting the 12 global stylesheets across route segments would cut it substantially, but they load in a fixed order that the Phase 40 cascade layers and ~1,464 `!important` declarations depend on, so it needs its own cycle with per-route visual regression rather than a drive-by change.
- **2026-07-26 [Phase 50 - Stop Shipping The Export Stack To Every Dashboard Visitor]**: Fortieth autonomous cycle, and the first after the owner redirected the loop toward *"real, noticeable improvements"* rather than cosmetic work. Cut **913 KB of uncompressed JavaScript** off the initial load of the two most-used authenticated pages.
  - **Found by following the build's own warning instead of the backlog.** `npm run perf:budget` had been printing `Largest top-level JS chunk: 911 KB` for dozens of cycles. Cross-referencing `.next/app-build-manifest.json` showed that chunk was loaded **eagerly by exactly two routes** — `/dashboard` (1368 KB total) and `/mentor` (1319 KB) — making them by far the heaviest pages in the product.
  - **Root cause**: the four `Export*Button` client components each *statically* imported their generator (`pptxGenerator`, `docxExport`, `pdfTranscriptGenerator`, `xlsxExport`), and `AdminReportTable` imported the whole of `exceljs` at module top level. So every student opening their dashboard downloaded the complete PPTX + DOCX + PDF + XLSX generation stack before ever clicking an export button.
  - **Fix**: the generator import moved inside each `onExport` handler as `await import(...)`, with the options interfaces switched to `import type` so they erase at build time. `ExportActionButton` already declared `onExport: () => void | Promise<void>` and already `await`ed it, so nothing in the calling contract had to change.
  - **Measured against `main`, per route** — `/dashboard` **1368 → 455 KB (−67%)**, `/mentor` **1319 → 406 KB (−69%)**, `/dashboard/analitik` **790 → 389 KB (−51%)**. The `perf:budget` dashboard figure went **1766 → 853 KB**. The 911 KB chunk still exists but is now fetched only when someone actually exports.
  - **The first pass only fixed half of it, and the numbers said so.** After converting the four buttons, `/mentor` dropped 913 KB but `/dashboard` was **byte-identical**. Rather than report a partial win, traced the remaining pull to `AdminReportTable.tsx:5`'s bare `import ExcelJS from "exceljs"` — `next/dynamic` on the component was not enough, because the library still sat in the route's chunk graph. Converting that too is what moved the dashboard.
  - **Verified the exports still work, since this changes runtime behaviour in five files.** A temporary probe route rendered all four buttons and Playwright clicked each one, capturing the actual download: **xlsx 8,443 bytes, pptx→pdf 15,632 bytes, docx 9,606 bytes, pdf 10,377 bytes**, plus `AdminReportTable`'s own multi-sheet export at 7,788 bytes. All real, non-empty files; zero console errors.
  - **Verified**: typecheck (exit 0), changed-file lint, `dup:check` (1.00%), production build (54/54 routes), perf budget above.
  - **Correction recorded in the same cycle**: an earlier claim that `.auth-visual`'s `#0d4b50` was "the largest off-palette surface in the product" was **wrong**. The panel is filled edge-to-edge by its own `<img>`, so that colour is only a load-state fallback, and the overlay above it already computed to brand blue. The mistake was reading `backgroundColor` off an element covered by its own image and treating the value as visible.
- **2026-07-26 [Phase 49 - Forum Reply Hover Parity (Last Phase 20 Backlog Item)]**: Thirty-ninth autonomous cycle, closing the final unimplemented item from the Phase 20 design/animation audit. On `app/forum/[threadId]/page.tsx` the original-post card carried `forum-thread-card hover-lift` (`:51`) while the reply cards 30 lines below used the same `forum-thread-card` **without** `hover-lift` (`:80`) — visually identical cards on the same page behaving differently.
  - **Checked whether adding the effect was right before adding it, because the opposite call was made two cycles earlier.** Phase 46 *declined* to add hover to `.al-activity-frame` on the grounds that hover affordances imply interactivity, and these forum cards are likewise non-interactive. The deciding difference is that `hover-lift` is already an established house convention here — **74 usages** across `app/` and `components/`, including plainly non-interactive containers (leaderboard podium cards, error cards, mentor stat articles) — and it is applied to the sibling card in the very same component. So the replies were *missed*, not the original post *wrong*; the consistent fix is to add it. Removing it from the OP card instead would have meant reversing a broad existing convention off the back of one file.
  - **Verified hover parity in a browser** via a temporary probe route (the thread page needs a live DB): both cards go `transform: none` → `matrix(1, 0, 0, 1, 0, -2)` on hover, identical. Note `styles/master.css` declares `.hover-lift` **twice** (`:47` at −4px and `:1853` at −2px); the later one wins, which is why −2px is the correct expected value.
  - **Verified**: typecheck (exit 0), changed-file lint, production build (54/54 routes), perf budget (799/793/1766 KB), zero console errors.
  - **The Phase 20 audit backlog is now fully closed** — its items were shipped or explicitly retired across Phases 20, 24, 43, 46 and 49.
- **2026-07-26 [Phase 48 - Finish The Teal Purge Across The Remaining Five Stylesheets]**: Thirty-eighth autonomous cycle, completing the sweep Phase 47 scoped to `taste.css`. Converted the remaining **43 off-palette teal literals** in `master.css` (16), `design-system.css` (11), `premium.css` (8), `pro-lms.css` (5) and `enterprise-lms.css` (3), using the same mapping and preserving every alpha.
  - **Two of the 44 matches were inside comments, and a blind regex sweep would have corrupted them.** `master.css:2372` and `premium.css:33` are Indonesian comments documenting the Phase 36 focus-ring decision, both of which *mention* `#0d9488` as prose — and they turned out to be the **only** two occurrences of that hex in the codebase. The replacement pass therefore computes `/* … */` spans first and skips any match inside one, so the record of a past decision stays intact rather than being silently rewritten into something that no longer makes sense.
  - **Deleted `--shadow-glow-teal` rather than renaming or leaving it.** After conversion the token held a brand-blue value under a teal name; checking consumers showed **zero** `var(--shadow-glow-teal)` references anywhere in `styles/`, `app/` or `components/`, so it was dead weight with a misleading name. `--shadow-glass` was kept — it has one real consumer and its name is not colour-bound.
  - **Handled the one mint/teal pairing rather than half-converting it**: `enterprise-lms.css:1482-1483` set `color: #0b6f66` on `background: #dcfbf3`. Converting only the text would have left blue-on-mint, which is worse than the original, so the background moved to `--al-primary-light #eff6ff` alongside it (**6.64:1**).
  - **Verified at the rendered level, not just by grep** — the check that actually matters. A sweep across `/`, `/program`, `/masuk`, `/daftar` and `/verifikasi` walked every element's computed `color`, `backgroundColor`, `backgroundImage`, `borderTopColor` and `boxShadow` looking for the three old teal families: **zero hits on all five routes**. Grep alone would not have caught a literal reaching the page through a token or a shorthand.
  - **Regression suite green**: 67 focusable elements tabbed across 5 routes with **0 missing focus indicators**; hero CTAs gold-present with white secondary; Tailwind utilities resolving; `body` still `rgb(249,250,251)` / `rgb(16,21,25)` from Phase 47; sticky header `top: 0` at y=4000 on 390px with no horizontal overflow; typecheck, production build (54/54 routes), perf budget (799/793/1766 KB); zero console errors.
  - **The recurring bug class is now closed.** It had been patched one instance at a time across Phases 19, 20, 41, 43, 46 and 47; there are no off-palette teal literals left in `styles/` outside the two comments.
  - **Still open (measured, deliberately not swept)**: mint/light-teal companions remain — `enterprise-lms.css` `#eefcf8` (2×) and `master.css`'s `#dff7f4`/`#def6f3`/`#dff8f4`/`#d9efec` inside minified single-line rules. And the big one: `.auth-visual` in `master.css` paints the **entire left panel of the login and register pages** `#0d4b50`, measured live as `rgb(13, 75, 80)` — the largest off-palette surface left, and a design decision rather than a mechanical swap.
- **2026-07-26 [Phase 47 - Retire The Off-Palette Green Theme Layer In `taste.css`]**: Thirty-seventh autonomous cycle. Started on the "dashboard header-toolbar/quick-item transitions" backlog item, found the real defect underneath it, and followed that instead — the hover rule in question was `border-color: rgba(15, 118, 110, 0.38)`, i.e. **Tailwind teal-700**, off-palette.
  - **Scanning outward from that one line found 56 off-palette teal literals across six stylesheets** — the same leftover-rebrand bug class already patched one instance at a time in Phases 19, 20, 41, 43 and 46. The root cause was never the individual literals: `styles/taste.css` declares an **entire green-tinted design-token ramp** at `:root` — `--taste-accent: #0f766e` (used **34×**), plus green-cast `--taste-canvas`, `--taste-ink`, `--taste-body`, `--taste-muted`, `--taste-line`, `--taste-deep`, `--taste-accent-soft` and a green `--taste-shadow`.
  - **The highest-impact part was verified live, not inferred.** `taste.css` also carries `html, body { background: var(--taste-canvas) !important; color: var(--taste-ink) !important; }`, so **the entire product's page canvas and body text were green-tinted**: measured on `/`, `/program` and `/masuk` as `rgb(244, 246, 241)` and `rgb(28, 40, 37)`. After the fix all three report `rgb(249, 250, 251)` and `rgb(16, 21, 25)`.
  - **Mapped every token onto a value that already exists in the palette** rather than inventing replacements, so the change introduces no new colour: canvas → `--al-bg-gray #f9fafb`, ink → `--al-ink #101519`, body → `--al-body #374151`, muted → `--al-muted #6b7280`, line → `--al-border #e5e7eb`, accent → `--al-primary #2a6ba7`, accent-soft → `--al-primary-light #eff6ff`, deep → `--al-primary-dark #1e5a8f`, shadow → ink-based neutral. Hardcoded `rgba(15,118,110,X)` → `rgba(42,107,167,X)` and `rgba(23,60,55,X)` → `rgba(30,90,143,X)`, preserving every alpha.
  - **Checked for deliberate multi-hue treatments before sweeping**, because Phase 19 had explicitly preserved some: scanned for any declaration pairing a teal literal with a brand blue and found **zero**. `enterprise-lms.css`'s `.lms-final-box` teal→blue→black gradient was inspected by hand and left alone — it is in a different file and outside this cycle's scope.
  - **Scoped deliberately to `taste.css` only.** The other 44 literals are mostly `box-shadow` glows in `design-system.css`/`master.css`/`premium.css`/`pro-lms.css`; folding them in would have ballooned the diff without the sitewide payoff, so they are logged below instead.
  - **Verified**: sitewide body colour measurements above; 67 focusable elements tabbed across 5 routes with **0 missing focus indicators**; hero CTAs gold-present with `#38bdf8` at **0 hits** and white secondary intact; Tailwind utilities resolving; sticky header `top: 0` at y=4000 on 390px with no horizontal overflow; screenshots of `/`, `/program`, `/masuk`; typecheck, production build (54/54 routes), perf budget (799/793/1766 KB); zero console errors.
  - **Off-palette backlog measured this cycle, not yet converted**: ~44 teal literals remaining — `master.css` 17, `design-system.css` 11, `premium.css` 9, `pro-lms.css` 5, `enterprise-lms.css` 2 (20 of them are `box-shadow` glows, plus the `--shadow-glow-teal` and `--shadow-glass` tokens). Separately, `.auth-visual` in `master.css` paints the **entire left panel of the login/register pages** `#0d4b50`, a dark teal, measured live as `rgb(13, 75, 80)` — the largest single off-palette surface left in the product.
- **2026-07-26 [Phase 46 - Course-Card CTA: Off-Palette Mint + Dead Hover]**: Thirty-sixth autonomous cycle, taking the "course-card CTA button" item from the Phase 20 backlog. `.course-foot > a` is the arrow CTA on `CourseCard` — the most-reused card component in the product (catalog, dashboard, program pages).
  - **Two defects on one element, both confirmed by measurement before editing.** (1) `background: #e7f7f5` — a hardcoded **mint/teal-green**, measured live as `rgb(231, 247, 245)`. This is the same leftover-rebrand class of bug purged in Phase 19 (rgba teal literals) and Phase 20 (`.course-quality-badge` hex literals); this one survived both sweeps. It directly violates the one hard constraint on this loop. (2) **No hover feedback whatsoever** — background, colour and transform were byte-identical at rest and on hover, despite the element already carrying a `transition: 0.25s cubic-bezier(0.16, 1, 0.3, 1)` that therefore animated nothing.
  - **Fix**: rest background → `rgba(42, 107, 167, .08)`, the royal-blue glass-wash convention already established for `.course-quality-badge` in Phase 20; hover/focus → solid `var(--teal)` fill with a white icon (**5.57:1**) and a 2px translate **up and to the right**, which reinforces the `ArrowUpRight` glyph's own direction rather than being arbitrary motion. Rest-state icon contrast measured at 6.46:1.
  - **Skipped the tap-target change deliberately.** At 34×34px the CTA passes WCAG 2.5.8 (AA, 24×24) though not the AAA 44px, and the whole card is already clickable via the image and title, so the arrow is a redundant affordance. Growing it shifts `.course-foot` layout and deserves its own verification pass rather than riding along in a colour/hover fix.
  - **Also declined an item from the same backlog rather than forcing it**: "landing metrics-bar/activity-frame hover". `.al-activity-frame` is a non-interactive `<figure>` (image + caption, no link) and `.al-metrics` is a static readout — hover affordances signal interactivity, so adding them would teach users these are clickable when they are not. Same reasoning as the Phase 45 `BuilderClient` correction.
  - **Verified in a real browser against a captured baseline**, using a temporary probe route because every page rendering `CourseCard` queries Prisma and no DB is available here. Before: `bg rgb(231,247,245)`, hover feedback **NO**. After: rest `rgba(42,107,167,0.08)`, hover `rgb(42,107,167)` with `rgb(255,255,255)` icon and `matrix(1,0,0,1,2,-2)`, hover feedback **YES**. Phase 36's focus ring confirmed intact on the same element (2px `rgb(42,107,167)` outline + 2px white inner ring) — first sampled at 250ms as a 1px outline and `1.999` shadow, which was a mid-transition artifact of the inherited `transition: all`, so it was re-sampled at 700ms rather than reported as a regression.
  - **Verified**: typecheck (exit 0), production build (54/54 routes), perf budget (799/793/1766 KB), zero console errors.
- **2026-07-26 [Phase 45 - Backlog Correction: `BuilderClient` Should NOT Be Lazy-Loaded]**: Assessed the next queued performance item and **removed it from the backlog rather than implementing it**, because implementing it would have been a regression dressed as an optimization.
  - **The item** (queued since Phase 21): "`BuilderClient` (730-line course builder) statically imported instead of `next/dynamic`."
  - **Why it is wrong**: `app/mentor/courses/[courseId]/builder/page.tsx` is an async **server component** whose entire content is `<BuilderClient />`. `next/dynamic` pays off when a heavy component is below the fold, conditionally rendered, one of several on a page, or gated behind an interaction — the last being exactly why it is correct for `CommandPalette` behind Ctrl+K (Phase 41). **None of those apply here**: the builder *is* the page, rendered immediately. With `ssr: false` the route would render nothing until JS arrives; with SSR retained, the same JS still ships, just in a chunk fetched *after* the main bundle — a net round-trip added to time-to-interactive.
  - **Also not supported by measurement**: the route builds at **8.33 kB / 127 kB First Load JS**, well inside every budget. There is no bundle problem to solve.
  - Recorded here so a later cycle does not re-pick it. The general lesson: "large component, not lazy-loaded" is not by itself a performance finding — the deciding question is whether the user needs it on first paint.
- **2026-07-26 [Phase 44 - Fix The Ragged Hero Headline Highlight]**: Thirty-fifth autonomous cycle, closing the Phase 31 defect that had been deferred through four redesign cycles: *"the `<mark>` colour-crop on the headline breaks into two ragged mismatched blue blocks with the '!' orphaned outside."* This is the single most prominent element in the product.
  - **Measured the defect across six viewports instead of trusting the four-phase-old description** (the hero has changed since, notably in Phase 39). Confirmed and quantified: the highlight split into **2 fragments at every width from 768px to 1920px**, with the two blocks' widths differing by **26–50px**, because "lebih jernih" wrapped between its two words at every desktop size. Only at 390px did it render as one block.
  - **Chose the fix by live-patching four candidates in the browser and measuring, not by reasoning** — the same method that produced the one-property `.al-hero-visual` fix in Phase 31. `white-space: nowrap` collapsed it to a single fragment at **every** width with **zero overflow** anywhere (verified down to 320px, where the block is 248px inside a 320px viewport), and as a bonus tightened the headline from 3 lines to 2 at 1440/1920 (h1 455px → 353px). `text-wrap: balance` was rejected on evidence: it fixed nothing at 1440px or 1920px, the two widths that mattered most.
  - **The screenshot caught two things the measurements did not**, which is why a visual change is never signed off on numbers alone: (1) the "!" still sat outside the blue block, visually stranded — moved inside the `<mark>`, which is also the second half of the original Phase 31 complaint; (2) the block's background box **overlapped the italic sub-line below it by 1–3px** at 1280px and up. That sub-line's `margin-top` was a flat `10px` that did not track the clamped type scale; `0.6em` was picked by measuring four candidates and gives a consistent **9–12px from 390px to 1920px** (previously −3px to +5px).
  - **Deliberately did NOT change `line-height: 0.98`.** The same inline-background geometry clips the descenders of the line above by **26px**, because an inline element's background paints its font-metric content box, which overlaps the neighbouring line whenever `line-height < 1`. Measured the fix: eliminating it needs `line-height ≥ 1.25`, growing the h1 from 357px to ~440px (**+23%**) and undoing the tight editorial display type this direction was built on. That is an art-direction trade-off, not a bug fix, so it is recorded here rather than bundled unverified into a PR that fixes something else.
  - **Verified**: typecheck, changed-file lint, production build (54/54 routes), perf budget (799/793/1766 KB), fragment/gap measurements at 320/360/390/768/1024/1280/1440/1920px, desktop + mobile screenshots, `docOverflow: 0` everywhere, zero console errors.
- **2026-07-26 [Phase 43 - Repair The Dead Dashboard Trophy Shimmer]**: Thirty-fourth autonomous cycle. With the audit roadmap's Phase 1 fully closed (items 01, 03–08; 02 was backend and out of scope), picked up the oldest unimplemented item from the Phase 20 design/animation audit backlog: the `shimmer-swipe`/`shimmer-sweep` keyframe-name bug in `styles/pro-lms.css`.
  - **The animation had been completely dead, and Phase 32 is why it stayed dead.** `.pro-shimmer-trophy::after` declared `animation: shimmer-swipe 4s infinite`, but no `@keyframes shimmer-swipe` existed anywhere. Phase 32's keyframe sweep deleted only keyframes whose names no longer appeared in any surviving `animation` declaration — so the correctly-named keyframe was garbage-collected *because* the typo meant nothing referenced it. The chip is live on the student dashboard (`app/dashboard/page.tsx:210`).
  - **Proved it was broken before fixing it**, by stashing the change and measuring `main`: `getAnimations({subtree:true})` returned **count 0** (a keyframe that doesn't exist registers no animation at all) and the `::after` transform was **identical across all 5 samples**. After the fix: **1 animation named `shimmer-swipe`, `playState: running`**, and tight sampling across a full cycle (32 samples @150ms) showed the sweep travelling the full **−46px → 96px** range with **13 distinct positions**, resting ~2.4s, then repeating. A first, looser sample had shown only "2 distinct of 5" — that was a sampling artifact from landing in the rest phase, so it was re-measured rather than reported as-is.
  - **Rebuilt the sweep on `transform` instead of `left`** rather than just restoring the missing keyframe: the original animated `left: -100% → 200%`, which forces layout on every frame. The `::after` is 50% of the chip's width, so `translateX(-200%) → 400%` of its own width spans exactly the same travel on the compositor. Same visual, no per-frame layout — which matters under a directive that names performance explicitly.
  - **Third instance of the same off-palette sky-blue.** `.pro-shimmer-trophy:hover` set `border-color: #38bdf8` — the colour removed from the hero in Phase 39 and from the command palette in Phase 41, still living here. Measured on `main` as `rgb(56, 189, 248)`, now `rgb(243, 180, 68)` (`--al-accent` gold), which is in-palette and also semantically apt for a trophy.
  - **Checked reduced-motion coverage rather than assuming it**: `styles/master.css:1942` already carries a global `*, *::before, *::after { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important }` block, so no new media query was needed. Verified in a `reducedMotion: 'reduce'` context — **0 animations registered, transform static** at the base `-48px`, while the hover colour still applies.
  - **Verified no keyframe-name collision** before adding `@keyframes shimmer-swipe` (four unrelated `@keyframes shimmer` exist across other stylesheets; none named `shimmer-swipe`).
  - **Verified**: typecheck (exit 0 from a clean `.next-dev`), production build (54/54 routes), perf budget (799/793/1766 KB), plus the before/after browser measurements above with zero console errors in both motion modes.
  - **Still open from the Phase 20/21 backlogs**: landing metrics-bar/activity-frame hover, dashboard header-toolbar/quick-item transitions, course-card CTA button, forum reply-card hover-lift, and `BuilderClient` (730-line course builder) statically imported instead of `next/dynamic`. From Phase 31: the `<mark>` colour-crop on the hero headline still breaks into ragged mismatched blocks, and the Fraunces→Inter seam between the hero and the next section.
- **2026-07-26 [Phase 42 - Delete The Last Broad-Selector `!important` (Roadmap Item 08)]**: Thirty-third autonomous cycle, and the item the audit called "the proof is one line". Deleted `styles/premium.css`'s `h1, h2, h3, h4, h5, h6 { font-family: inherit !important; }` — a blanket rule with element-level specificity but `!important`, which therefore beat **every** non-important heading `font-family` in the product regardless of specificity, across all 12 stylesheets.
  - **It was hiding a real, user-facing bug — found by scanning every heading rule rather than by assuming this was pure cleanup.** `styles/master.css` sets `.certificate-paper h2 { font-family: Georgia, serif }`, and that `h2` is **the recipient's name on every certificate** — the single most prominent element on a formal document. Because the blanket rule out-ranked it, the name has been rendering in **Inter** the whole time. Baseline measurement in a browser confirmed it before any edit: `#cert-h2 -> Inter`.
  - **Fixed toward one serif, not two.** Rather than let deletion silently reintroduce Georgia — a third typeface alongside Inter and Fraunces — the rule now resolves to `var(--font-fraunces, Georgia, serif)`, the display serif already established by the Field Dossier direction (hero, ledger metrics, FAQ). Georgia is retained only as the fallback.
  - **Removed the two `!important` workarounds the deleted rule had forced**, rather than leaving them as dead weight: `.al-page .al-hero-title` (`akseslegal-theme.css:212`, added in Phase 26 specifically to defeat this rule) and `.al-page .al-faq-intro h2` (`home-landing.css:149`). Both still win on plain specificity — `(0,2,0)` and `(0,2,1)` against `.al-page h1..h6`'s `(0,1,1)`, all within `@layer legacy` — which was verified by measurement, not by reading the numbers off the page.
  - **Deliberately left `.al-metrics b`'s `!important` alone**: `<b>` is not `h1`–`h6`, so that declaration was never competing with the deleted rule. Removing it would have been an unrelated change with its own risk.
  - **Verified against a captured `main` baseline, not a remembered one** — the discipline that caught the Mantine regression last cycle. Ten heading targets measured across `/`, `/program`, `/masuk`, `/daftar`, plus a temporary probe route for the certificate markup (that page needs a live DB, so its cascade was exercised in isolation). Result: **nine of ten byte-identical to `main`**, with the single intended change being `#cert-h2` Inter → Fraunces. Hero title and FAQ heading stayed Fraunces after losing their `!important`, confirming specificity alone carries them.
  - **Full regression suite green**: 67 focusable elements tabbed across 5 routes with **0 missing focus indicators**; hero CTAs still gold-present / `#38bdf8`-absent (0 hits) with primary gradient vs white secondary distinct; Tailwind utilities still resolving (`flex`, `gap-2` → 8px, `text-emerald-600` → `rgb(5,150,105)`); sticky header `top: 0` at y=4000 on 390px with no horizontal overflow; zero console errors.
  - **`!important` in `styles/`: 1465 → 1464**; `premium.css` 247 → 246. A single line, but the only one that could override a component library on bare element selectors — which is what made it the audit's unlock item.
  - **Verified**: typecheck, production build (54/54 routes), perf budget (798/793/1766 KB — unchanged), plus the browser suites above.
- **2026-07-26 [Phase 41 - Rejected Mantine On Measurement; Gave CommandPalette A Real Focus Trap]**: Thirty-second autonomous cycle, and the first time this loop **abandoned its own previous cycle's direction because the numbers refuted it**.
  - **The Mantine foundation (PR #33) was reverted, not merged.** The plan was to adopt Mantine v8 as the component library and convert surfaces to it. CI's `npm run perf:budget` failed, and measuring against `main` showed why: `MantineProvider` in the root layout added **+759 KB uncompressed to every route** (landing 798 → 1557 KB). Adding `optimizePackageImports: ["@mantine/core"]` recovered most of it but still left **+225 KB on every route** — including the landing page, where no Mantine component renders. Paying that permanently to gain a focus trap in a Ctrl+K palette is indefensible under a directive that names performance and loading time as in scope. Raising the budget to accommodate it would have been exactly the "superficial fix" the owner ruled out, so the budget was left alone and the dependency dropped.
  - **Corrected a false claim I had made in the previous cycle.** PR #33 asserted "bundle verified unchanged: First Load JS shared by all = 103 kB before and after." That number is real but measures the wrong thing — Next's "shared by all" line excludes the layout's own client-component chunks, which is precisely where `MantineProvider` landed. The route-level budget script caught what the headline number hid.
  - **Solved the actual problem with a headless primitive instead**: `@radix-ui/react-dialog` (1.1.23) in `components/ui/CommandPalette.tsx`. Headless matters here beyond size — a *styled* library competes with this repo's 15,600 lines of bespoke CSS, while an unstyled primitive composes with it, so every existing inline style survived untouched. Cost: **0 KB added to landing/player/dashboard** (all three byte-identical to `main`), because the palette is already behind `next/dynamic({ ssr: false })` — Radix lands in a **37 KB lazy chunk fetched only when the palette opens**.
  - **What the hand-rolled version actually lacked** (it already had keyboard nav and `role`/`aria-*` wiring from Phase 23): no focus trap, no scroll lock, no `aria-hidden` on the background, no focus restore. Radix supplies all four. Also deleted the global `window` keydown listener in favour of a handler on the dialog itself — safe now that focus is trapped — and let Radix own Escape.
  - **Found and fixed a real focus-restore bug that only surfaced by testing.** Radix's *modal* Content deliberately overrides `onCloseAutoFocus` to `preventDefault()` and focus `context.triggerRef` — but this palette is opened externally (Ctrl+K, or a dashboard button) and renders no `<Dialog.Trigger>`, so that ref is `null` and focus was dumped on `<body>`. My first hypothesis (React's `autoFocus` racing FocusScope) was **wrong** — removing it changed nothing, so it was instrumented rather than guessed at again. Fixed by tracking the pre-open focus target via `focusin` and restoring it in `onCloseAutoFocus`. A second bug hid behind the first: sampling `document.activeElement` eagerly on effect setup re-ran the instant `isOpen` flipped false, capturing the palette's own input mid-unmount (`isConnected: false`) and clobbering the trigger — so the tracker is now driven purely by real `focusin` events and ignores anything inside `[role="dialog"]`.
  - **Two off-palette colours removed from the same file** (the palette is the one hard constraint): the panel glow was `rgba(13, 148, 136, 0.3)` — the **pre-rebrand teal** purged everywhere else in Phase 19, still living here — and the selected-row title/arrow were `#38bdf8`, the same off-palette sky-blue removed from the hero in Phase 39. Both replaced with real tokens (brand blue glow; `--al-accent` gold `#f3b444`). The gold also fixes a genuine contrast failure: the footer wordmark was `#2a6ba7` on the near-black panel at **3.25:1** (fails), now **9.78:1**.
  - **Verified in a real browser, and the checks themselves were corrected mid-run.** First pass produced three bogus results — the "baseline" was captured *after* opening, the scroll-lock test used programmatic `window.scrollTo` (which no scroll-lock library intercepts; wheel/touch is the real behaviour), and the colour probe selected the wrong node. Rebuilt properly: **focus trap — 25 Tab presses, 0 escapes**, cycling input ↔ close button; scroll lock — wheel gesture moves 600px while closed, **0px while open**, working again after close; `body` overflow `visible → hidden → visible`; background `aria-hidden="true"` applied and removed (28 nodes); focus restored to the trigger on Escape (`restoredToTrigger: true`); arrow-key nav and `aria-activedescendant` still working; **0 off-palette hits** for `#38bdf8`/old teal; zero console errors.
  - **Verified**: typecheck, lint, `dup:check` (1.00%), production build (54/54 routes), perf budget (798/793/1766 KB — identical to `main`), plus the browser suite above.
  - **Method note carried forward**: `next build`'s "First Load JS shared by all" is not a bundle-regression guard. Route-level `npm run perf:budget` against a **`main` baseline** is — and a baseline must be *measured*, never remembered.
- **2026-07-26 [Phase 40 - Cascade Layers (Roadmap Item 07)]**: Thirty-first autonomous cycle, and the "unlock" item the audit named. Introduced native CSS cascade layers across all 12 globally-imported stylesheets — previously **zero** `@layer` usage anywhere. Order declared once at the top of `styles/typography.css` (the first import): `@layer reset, tokens, legacy, components, routes;`. Tailwind's Preflight goes into `reset` (it *is* a reset, so it belongs lowest); `@tailwind components` into `components`; **`@tailwind utilities` deliberately left UNLAYERED**, because unlayered normal declarations beat every layer — which is exactly the precedence utilities are supposed to have. The 11 legacy stylesheets are wrapped in `@layer legacy`, and `components` is reserved for the component library landing next cycle.
  - **Corrected my own stated rationale before shipping.** The plan claimed layering would "fix legacy `!important` beating Tailwind." That is **false**, and I verified it in a browser rather than reasoning: layers do *not* defeat `!important` — for important declarations the layer order **inverts** (earlier layers win) and important always beats normal. Test page confirmed `@layer legacy { color: red !important }` still beats both `@layer components { color: green }` and an unlayered `color: blue`. What layering actually buys is narrower and still worth it: legacy **normal** declarations stop beating `components` normal declarations, which is precisely what makes a component library viable here.
  - **Measured the real risk surface rather than assuming it.** Of ~1,465 `!important` declarations, only **one** sits on a broad element selector that could collide with a component library's own `<button>`/`<input>`/heading markup: `premium.css` `h1,h2,h3,h4,h5,h6 { font-family: inherit !important; }` — the exact line the audit called "the proof is one line," already queued as roadmap item 08. Every other `!important` is scoped to project-specific classes (`.al-page …`, `.dashboard-fresh …`) that will not collide.
  - **Verified chunk order, which is the non-obvious failure mode.** Next.js splits this CSS into 6 chunks, and layer precedence is set by whichever comes first — the explicit declaration or a first-encountered `@layer` block. Confirmed from the built page that the chunk carrying the declaration (`46fecdfe…css`) is the **first** `<link>`, so the declared order wins.
  - **Verified in a real browser, all previously-shipped fixes intact**: Phase 39 hero CTAs (gold `rgb(243,180,68)` present, off-palette `#38bdf8` absent, primary vs secondary distinct); Phase 36 focus rings (**60 focusable elements tabbed across 5 routes, 0 without an outline**); Phase 38 sticky header (`top: 0` after scrolling to y=4000 at 390px) and no horizontal overflow (390 === 390); Tailwind utilities still applying (`flex` → `display:flex`, `gap-2` → `8px`, `text-emerald-600` → `rgb(5,150,105)`). Zero console errors. Also typecheck and a production build (54/54 routes).
  - **Method note worth keeping**: when checking emitted CSS, grep **all** of `.next/static/css/*.css`, not the largest chunk. Earlier in this session I grepped a single chunk, saw no `@layer` and no Preflight, and wrongly concluded Tailwind had swallowed them — a false alarm that nearly redirected this work toward an unnecessary `@import layer()` workaround.
- **2026-07-26 [Phase 39 - Delete The ULTRA-SHARP OVERRIDES Block (Roadmap Item 04)]**: Thirtieth autonomous cycle, and the highest-risk item in the audit's Phase 1. Deleted the 74-line "ULTRA-SHARP FONT CLARITY & BUTTON STANDARDIZATION OVERRIDES" block at the end of `styles/akseslegal-theme.css` — every declaration in it was `!important`, and it silently overwrote the redesign that sits above it in the same file. `!important` in that file: **161 → 87**.
  - **The audit under-stated this too — same pattern as Phase 36's focus rings.** Deleting the block alone would *not* have fixed the "two hero CTAs are identical" regression, because `.al-btn-green`'s own base rule at `:305` independently hardcoded `background: linear-gradient(135deg, #1e5a8f, #2a6ba7) !important` — the same blue as `.al-btn-primary` — plus an off-palette `#38bdf8` sky-blue hover. Found by reading the base rules *before* deleting rather than assuming the override was the only cause.
  - **Fixed each cause at source instead of re-patching**: (1) `--al-body` raised `#4b5563 → #374151` — this addresses the washed-out-text complaint the block was written for, at **8.9:1** on white, with no weight inflation; (2) the block's one legitimate intent (font smoothing) kept as `-webkit-font-smoothing: antialiased` **once on `.al-page`** and inherited, rather than `.al-page * { … !important }` on every element; (3) `.al-btn-green` given a genuine secondary treatment — white surface, royal-blue text and hairline border — so the hero finally has a visible primary/secondary hierarchy.
  - **All six documented regressions verified in a real browser**, not from the diff: ink-bleed CTA now computes `linear-gradient(90deg, rgb(243,180,68) → rgb(42,107,167) → rgb(30,90,143))` with **zero** `#38bdf8`; the two hero CTAs differ (gradient vs `rgb(255,255,255)`); the final conversion button is white-on-dark-blue, not blue-on-blue; the metrics number `rgb(42,107,167)` differs from its caption `rgb(107,114,128)`; the trust row is weight **500**, not 750; and paragraphs render at weight **400**, not 600. Zero console errors.
  - **Verified**: typecheck, production build (54/54 routes), the six browser measurements above, desktop screenshot.
- **2026-07-26 [Phase 38 - Restore Sticky Headers (Roadmap Item 06)]**: Twenty-ninth autonomous cycle. Three independent `overflow-x: hidden` declarations (`premium.css:340` on `html, body`, plus the `max-width: 100vw !important` beside it; `enterprise-lms.css:55` on `body`; `akseslegal-theme.css:36` on `.al-page`) each silently promote their own element into a new containing block for descendant `position: sticky` elements — so `.al-page .site-header`, `.dashboard-header`, and the player header never actually stuck to the viewport, they just scrolled away like any static element. On a landing page that reaches ~13,500px on mobile, the hamburger menu — the only navigation on a touch device — disappeared for the entire scroll after the first screen.
  - **Fix**: deleted all three declarations. No replacement needed — none of them existed for a legitimate horizontal-overflow reason that testing could find.
  - **Verified the actual risk of this change, not just that it builds**: real-browser check at 390px across `/`, `/program`, `/masuk` confirms `document.documentElement.scrollWidth === window.innerWidth` on all three (**no horizontal overflow introduced**), then scrolled the landing page to `y = 4000` on mobile and `y = 3000` on desktop and confirmed `.site-header`'s `getBoundingClientRect().top` stayed at **0** in both cases — it had drifted to whatever the scroll offset was before this fix.
  - **Verified**: typecheck, production build (54/54 routes), the browser checks above.
- **2026-07-26 [Phase 37 - Fix Collapsed Lesson Text (Roadmap Item 03)]**: Twenty-eighth autonomous cycle. `.player-desc-read` (the TEXT/DOCUMENT lesson body in `CoursePlayer`) rendered mentor-authored content as a single raw text node with no `white-space` rule, so every paragraph break and line break a mentor typed collapsed into one unbroken wall of text — the exact failure the audit documented. `.discussion p` in `master.css` had the identical gap.
  - **Fix**: `white-space: pre-wrap` on both, matching the pattern already established by `.forum-post-content`/`.forum-reply-content` in the same file (`design-system.css:3330`) — not a new convention, just applying an existing one to the two places it was missing. Deliberately did **not** add a markdown renderer, which the audit also suggested: that needs a new npm dependency and is out of scope for a one-property CSS fix.
  - **Verified**: typecheck, production build (54/54 routes).
- **2026-07-26 [Phase 36 - Restore Keyboard Focus Product-Wide (Roadmap Item 01)]**: Twenty-seventh autonomous cycle; first *design* item from the delivered audit roadmap.
  - **The audit under-stated this.** It reported one `!important` rule killing three correctly-authored focus rings. Measuring the actual composited colours showed **all four existing indicators fail WCAG 2.2's 3:1 minimum for non-text contrast** — so merely deleting the override would have left the "winners" failing too: `enterprise-lms.css:87` blue @18% = **1.29:1**; `premium.css:33` teal @15% = **1.19:1**; `home-landing.css:12` blue @42% = **1.86:1**; `lms-fresh.css:45` accent @34% = **1.64:1**. A solid `--al-primary` outline is **5.59:1**.
  - **Fix — one two-ring pattern, applied consistently**: `outline: 2px solid` + `outline-offset: 2px` + `box-shadow: 0 0 0 2px #ffffff`. The white inner ring fills the offset gap so the indicator stays visible on dark/blue surfaces (pathway card, final conversion button) where a blue outline alone would vanish. Applied at all three specificity tiers that exist (`enterprise-lms.css` global, `.al-page` in `home-landing.css`, `html body .dashboard-fresh` in `lms-fresh.css`) so the higher-specificity rules agree instead of competing.
  - **Removed five base-state outline resets**, each of which beat the global `:focus-visible` rule: `enterprise-lms.css:86` (`!important`), `premium.css:33`, `lms-fresh.css:466`, `master.css:69` (`.auth-form input`, `.catalog-search input`), `master.css:2370` (`input[type=text|email]:focus`, specificity 0,2,1), `home-landing.css:519` (`.al-page .verification-input-row input:focus`). Three of these also carried **off-palette teal `#0d9488`** halos at 10–15% opacity. Net `outline:none !important` in `styles/`: **0**.
  - **Found by tab-through, not code review.** The last three offenders were invisible to grep-and-reason because they win on *specificity*, not `!important` — only walking the real focus order surfaced them. Final: **67 focusable elements across `/masuk`, `/daftar`, `/`, `/program`, `/verifikasi` — 0 without a visible outline.**
  - **Two false failures correctly discarded rather than chased**: (1) the hero CTA appeared to fail until re-measured — a probe artifact from reading computed style 90 ms after `Tab`; the settle was raised to 250 ms. (2) `nextjs-portal` has no outline, but that is Next.js's **dev-only error overlay** and does not exist in production, so it is excluded rather than "fixed".
  - **Verified**: typecheck, production build (54/54 routes), real-browser tab-through above.
- **2026-07-26 [Phase 35 - Remove Live Admin Credentials From The Production Login Page]**: Twenty-sixth autonomous cycle, and the first item executed from the newly-delivered 9-lens design audit's roadmap. Taken **out of the normal design-polish order deliberately**: the audit surfaced a genuine security exposure, and tuning focus rings while working admin credentials sat on the production login page would have been indefensible.
  - **The exposure**: `components/shared/AuthForm.tsx` rendered `<small>Kata sandi semua akun: profas123</small>` plus three buttons that auto-fill `peserta@`/`mentor@`/**`admin@profas.id`** — the last being a **SUPER_ADMIN** account. The block was gated only on `mode === "login"`, with no environment check whatsoever, so anyone who opened the live login page could read working administrator credentials.
  - **Fixed by gating on `process.env.NODE_ENV !== "production"` rather than deleting.** This is strictly stronger than deleting the markup *and* preserves local dev convenience: Next.js statically replaces `process.env.NODE_ENV` at build time, so the entire branch is removed by dead-code elimination — the password string never reaches the client bundle at all, rather than being merely hidden by CSS or skipped by a runtime check. Hoisted the flag and the password to module-scope constants (`SHOW_DEMO_ACCOUNTS`, `DEMO_PASSWORD`) so the elimination is unambiguous to the bundler, and added a small `dev` badge (`.demo-box-env`, royal-blue, `styles/design-system.css`) so it is obvious the box is non-production when it *does* render.
  - **Verified by grepping the built output, not by reading the diff** — the whole point of this fix is what ships, so inspecting the source would have proven nothing. In `.next/static` (everything served to a browser): **0** occurrences of `profas123`, **0** of `admin@profas.id`, **0** of `"Akun demo"`. Control check in the same run: `"Masuk ke Dashboard"` appears **2×**, confirming the search actually works against the minified bundle. Three residual `profas123` hits exist under `.next/cache/webpack/*.pack` — build-time webpack cache, never served and not part of deployed output.
  - **Still needs the project owner (cannot be done from code)**: the three demo passwords are live credentials in the database and should be **rotated**, since they were publicly readable for as long as the box shipped. Changing them means editing seeded/production user rows, which is data work and outside this loop's scope.
  - **Verified**: typecheck, changed-file lint, production build (54/54 routes).
- **2026-07-26 [Phase 34 - Extract `useReducedMotion` Hook; Skip Confetti Burst]**: Twenty-fifth autonomous cycle, completing the motion-safety work started in Phase 33 (still direction-agnostic, so it cannot conflict with the pending audit roadmap).
  - **Extracted `hooks/useReducedMotion.ts`** from the inline copy added to `ScrollStack.tsx` last cycle, now that there is a third consumer — follows the existing `hooks/` convention (`useLocalBackup.ts`). Documented *why* it must exist in JS at all: CSS `@media (prefers-reduced-motion: reduce)` can only neutralise CSS animations/transitions, never a JS scroll hijack, inline per-frame `transform`/`filter` writes, or elements whose *presence in the DOM is itself the animation*.
  - **`CompletionCelebration.tsx`**: the course-completion overlay rendered **40 randomised confetti particles** falling full-screen. That is a textbook vestibular trigger, and because the particles' existence *is* the animation, no CSS reduced-motion block could suppress it. Now skipped entirely when the preference is set — while the celebration itself (checkmark, "Selamat!", certificate countdown) renders fully intact, so nothing informational is lost.
  - **Deliberately did NOT touch `RouteProgressBar.tsx`**, despite it being on the queued list. Reduced-motion means avoiding large/parallax/vestibular motion, not stripping all feedback: a thin top-of-page route progress bar is a legitimate *state indicator*, and freezing it would make it less informative while removing no motion risk. Blanket-applying the flag to everything that moves would have been the lazier read of the guideline.
  - **Verified**: typecheck, changed-file lint, production build (54/54 routes), and a real-browser re-run of the Phase 33 both-modes check to confirm the hook extraction was behaviour-preserving — results identical to before the refactor (`no-preference`: `<html class="… lenis">`, cards carry `translate3d(…) scale(1) rotate(0deg)`, wheel delta 399; `reduce`: no `lenis` class, all transforms `none`, wheel delta 400; zero console errors in both).
  - **Noted, not changed**: the confetti palette (`#f59e0b`, `#ef4444`, `#3b82f6`, `#10b981`, `#8b5cf6`, `#ec4899`, `#06b6d4`, `#f97316`) is entirely outside the royal-blue palette. Unlike the Phase 30 emerald — which was a leftover on the site's most prominent headline — multi-colour confetti reads as a deliberate celebration choice, so it is an art-direction call for the audit rather than a drive-by fix.
- **2026-07-26 [Phase 33 - Honour `prefers-reduced-motion` in ScrollStack]**: Twenty-fourth autonomous cycle. Picked as direction-agnostic (every art direction must honour reduced-motion, so this cannot conflict with the pending audit roadmap) after auditing motion-safety coverage across all six JS motion components. `LandingScrollDirector.tsx`, `MetricsLedger.tsx` and `GlobalLeafStagger.tsx` all already checked the media query — **`ScrollStack.tsx` did not**, and it is by far the most aggressive motion in the product: it initialises **Lenis, which hijacks the entire window's scrolling** with momentum/easing, plus writes pin-and-scale `transform`/`filter` inline styles to every card on every scroll frame. Both are invisible to CSS: the seven `@media (prefers-reduced-motion: reduce)` blocks already in `styles/` can only neutralise CSS animations/transitions, never a JS scroll hijack or inline transforms. Hijacked scrolling is exactly the class of motion that triggers vestibular symptoms, and on the landing page it is page-wide rather than a decorative flourish a user can look away from.
  - **Implementation**: added a small `useReducedMotion()` hook in the component (subscribes to `matchMedia` `change`, so toggling the OS setting takes effect without a reload). When reduced: `setupLenis()` returns early so **Lenis is never constructed at all** (no scroll hijack, no rAF loop), the transform pipeline is skipped, and the cards get only their `marginBottom` spacing — with every animation-mode inline style (`transform`, `filter`, `willChange`, `perspective`, `backfaceVisibility`, `transformOrigin`) explicitly cleared, so no card can be stranded mid-scale/mid-blur if the preference is switched while the page is open. `prefersReducedMotion` added to both effect dependency arrays so the component fully re-initialises on change.
  - **Verified in a real browser in both modes** (Playwright `reducedMotion` context option) rather than by code review: with `no-preference`, `<html class="… lenis">` and cards carry `transform: translate3d(…) scale(1) rotate(0deg)` + `will-change: transform, filter`; with `reduce`, the `lenis` class is **absent entirely** and all three cards report `transform`/`filter`/`will-change` = none. Crucially, **scrolling still works identically** (wheel delta 399 vs 400) — the page is fully functional, just without hijacked momentum — and a screenshot confirms the cards render as a clean, readable vertical list. Zero console errors in either mode. Also typecheck, changed-file lint, and a production build (54/54 routes).
  - **Still uncovered (queued, lower impact)**: `CompletionCelebration.tsx` (confetti burst) and `RouteProgressBar.tsx` also lack a reduced-motion check. Both are short, non-scroll-hijacking, and fire on discrete events rather than continuously, so they rank well below ScrollStack.
- **2026-07-26 [Phase 32 - Delete Dead `pro-*` Theme (−769 lines), Untrack `next-env.d.ts`]**: Twenty-third autonomous cycle, chosen as deliberately *direction-agnostic* groundwork while the 9-lens design audit is still running — dead-code removal is required by any eventual art direction, so it cannot conflict with the roadmap.
  - **Measured dead-CSS across all 14 stylesheets** by cross-referencing every class selector against a deliberately permissive token scan of `app/`, `components/`, `hooks/`, `services/`, `utils/` (permissive ⇒ conservative "dead" calls). Result: `pro-lms.css` **77% unreferenced** (43 of 56 classes), `taste.css` 43%, `enterprise-lms.css` 32%, `master.css` 31%, `design-system.css` 26%. The unreferenced names reveal the accretion history: at least three *entire abandoned landing-page systems* still ship on every page (`master.css`'s `hero-*`, `taste.css`'s `lms-hero-*`, `enterprise-lms.css`'s `hero-*-clean`/`bento-card-clean`/`btn-cyan-*`), plus a dead neon/aurora theme in `pro-lms.css` (`pro-aurora-bg`, `pro-neon-border`, `pro-floating-orb`, `pro-hologram-seal`, `pro-dark-glass`) whose aesthetic actively contradicts the royal-blue direction.
  - **Removed this cycle**: `styles/pro-lms.css` **1,215 → 446 lines (−769, −63%)**. Scoped safely *by construction*: only top-level rule blocks where **every** class in the selector was both unreferenced **and** `pro-*`-prefixed were dropped, so any rule mixing in a live class (e.g. `.dash-card.pro-neon-border`, `.pro-glass-card .pro-neon-border`) was retained. Then swept `@keyframes` iteratively, deleting only those whose name no longer appeared in any surviving `animation` declaration. Confirmed first that **no `pro-*` class is ever built dynamically** (`` `pro-${…}` ``) — the codebase does construct classes dynamically elsewhere (`` `type-${…}` ``, `` `role-${…}` ``, `` `${x ? "done" : "active"}` ``), which is exactly why that check mattered before deleting anything.
  - **Near-miss worth recording**: the verification pass flagged `pro-badge` as possibly-removed-but-in-use. It turned out to be a false positive from substring matching — `pro-badge` never existed in `pro-lms.css` at all; the token came from `forum-pro-badge` (`design-system.css:2538`). Checked before concluding, so nothing was wrongly restored or wrongly deleted.
  - **Untracked `next-env.d.ts`** (gitignored). Next.js regenerates it on every run and its contents differ between dev and build because `next.config.ts:9` intentionally splits `distDir` (`.next-dev` vs `.next`), so the tracked copy flip-flopped on every `npm run dev` — it churned 6+ times in this session alone. **Verified rather than assumed** before untracking, since both CI workflows run `npm run typecheck` *before* `npm run build`: with the file deleted, `tsc --noEmit` still exits **0** (tsconfig doesn't depend on it) and `next build` regenerates it. Also gitignored root-level `_probe*`/`_audit*`/`_shot*` scratch artifacts.
  - **Verified**: typecheck, production build (54/54 routes), and live screenshots of `/`, `/program`, `/masuk` confirming zero visual regression and zero console errors.
  - **Remaining dead-CSS backlog (measured, not yet removed)**: the three abandoned landing systems above are ~300 more unreferenced classes across `master.css`/`taste.css`/`enterprise-lms.css`. Left for the audit's consolidation roadmap because, unlike the self-contained `pro-*` decorative rules, those files also contain *live* selectors interleaved with the dead ones and need per-route visual regression rather than a by-construction safety argument.
- **2026-07-26 [Phase 31 - Fix Hero Visual Column: One Root Cause, Three Defects]**: Twenty-second autonomous cycle. Three separate landing-hero defects measured in a real browser during the Phase 30 investigation all turned out to share a single root cause, fixed with a one-property change. Measured at 1280/1440/1920 after a 5s settle with `transform: none` (to rule out a mid-animation artifact — an earlier "blank mobile hero" suspicion had already proved to be exactly that, a font-loading timing artifact, and was correctly discarded rather than reported):
  - **Symptoms**: (1) `.al-hero-img-wrap` rendered at **261×274px inside a 511px-wide column** despite `width: 100%` — the hero photo, the single largest visual on the site, was displaying at ~25% of its intended area; (2) the two floating cards physically **overlapped by 123×82px**, the pull-quote clipping the ledger card's content; (3) the marquee strip **overhung the photo by 219px** on the right, floating in empty space.
  - **Root cause**: `.al-hero-visual` was `display: flex` with `justify-content/align-items: center`. Its *only* in-flow child is `.al-hero-img-wrap` — the exhibit tag, both floating cards, and the marquee are all `position: absolute` — so flex bought nothing, but flex centering combined with an `aspect-ratio` child made `width: 100%` be ignored and the box sized from intrinsic content instead (grid row was 881px, yet the column collapsed to 274px). Because the cards and marquee are positioned relative to that collapsed column, its collapse is also what drove them into each other and past the photo's edge.
  - **Fix**: `display: block`. Verified by live-patching the property in the browser *before* editing any file (261×274 → 511×537), then re-measuring after the edit at all three viewports: image **511×537**, card overlap **0×0 (`overlapping: false`)**, marquee overhang **−31px symmetric inset** (was +219px). Grid row height is unchanged at 881px, so nothing below the hero shifts. Mobile re-checked at 390px: image 314px, `scrollWidth === innerWidth` (no horizontal overflow), zero console errors.
  - **Verified**: typecheck, production build (54/54 routes), desktop + mobile screenshots.
  - **Still queued for the redesign cycles** (deliberately not patched ad hoc, so they land inside one coherent art direction rather than as conflicting patches): the `<mark>` color-crop on the headline still breaks into two ragged mismatched blue blocks with the "!" orphaned outside; the Fraunces→Inter seam between the hero and the very next section; ScrollStack cards that are 855×300px slabs holding ~15 words. The 9-lens product-wide design audit is still running and will sequence these.
- **2026-07-26 [Phase 30 - Fix Invisible "Peserta" CTA + Off-Palette Emerald; De-`!important` ScrollStack]**: Twenty-first autonomous cycle, and the first under the project owner's broadened directive: a full-product design evaluation with complete creative freedom over layout/typography/hierarchy/navigation/components/spacing/responsiveness/accessibility — the **only** fixed constraint being the existing color palette. (The owner also named `akseslegal.id` as the reference to surpass; that host is blocked by this session's egress policy — 403 on CONNECT, confirmed via the proxy status endpoint — so it could not be fetched. Noted rather than worked around. `styles/akseslegal-theme.css`'s own header already documents that the current theme was modeled on that site in an earlier phase, so the palette/DNA is already in the repo.)
  - **Critical user-facing bug found by actually rendering the site (not by code review), and fixed**: on the landing page's "Satu platform untuk seluruh alur LMS profesional" section, the **first pathway card ("Peserta" — the primary audience's entry point) rendered a white icon and a white "Masuk alur Peserta →" link on a white background, i.e. the section's main CTA was invisible.** Root-caused with CSSOM rule inspection in a real browser: `components/ui/ScrollStack.css` (a scroll-*mechanics* component) had grown a 15-declaration `!important` visual skin at lines 62–101, including `background: #ffffff !important`, which beat the non-`!important` blue gradient on `.al-pathway-card:first-child`. Its sibling rules `!important`-forced `h3`/`p` colors too — but **not** the icon `<span>` or the `<a>`, so those alone kept `color: #ffffff` from the first-child rule. White on white.
  - **Fixed at the root rather than papered over**: deleted the entire `!important` skin block from `ScrollStack.css` (18 `!important` declarations → 2, and the 2 that remain are legitimate window-scroll-mode overrides), and moved the geometry/typography it was enforcing into the theme layer on `.al-method-card`/`.al-pathway-card` in `styles/akseslegal-theme.css`, preserving the previously-rendered geometry exactly (max-width 860px, margin 25px auto, padding 3rem, radius 32px, h3 1.6rem/800, p 1.08rem/1.65, plus the mobile 1.8rem/24px/16px variant) — while swapping the off-palette Tailwind slate literals it used (`#0f172a`, `#475569`, `rgba(226,232,240,.9)`) for the real tokens (`--al-ink`, `--al-body`, `--al-border`). Making the gradient visible then exposed a second, latent contrast failure: a blanket `.al-page .al-pathway-card p { color: #1f2937 !important }` at `akseslegal-theme.css:1706` painted dark text on the now-blue card; narrowed that selector to `:not(:first-child)` so the highlighted card inherits its intended white text — deliberately fixing it by *reducing* the sledgehammer's reach rather than adding a competing `!important`.
  - **Palette integrity (directly serves the owner's one constraint)**: `.al-hero-title span` — the "Belajar. Mencoba. Berdampak." line, the most prominent copy on the site — was painted by `linear-gradient(135deg, var(--al-primary) 0%, #10b981 100%)`, i.e. a hardcoded **emerald green** outside the palette. A prior attempt to fix this (`color: #1e5a8f !important` at `:1706`-ish) had never worked, because `-webkit-text-fill-color: transparent` always beats `color`. Replaced the gradient/background-clip hack with a plain `color: var(--al-primary-dark)`, which also retires that dead override.
  - **Audit evidence gathered this cycle (measured, for the queued design-system work)**: 15,600 lines of CSS across 12 globally-imported stylesheets; **1,492 `!important` declarations**; 7 competing `:root` token blocks defining 172 custom properties. The documented palette is largely fiction in practice — the Tailwind slate ramp is used **280×** vs **28×** for the real `--al-*` neutrals (10:1), alongside 6 blue families and 6 greens. Computed WCAG contrast: `--al-accent` #f3b444 on white = **1.84:1** and white-on-gold = **1.84:1** (both fail badly; gold is only safe as a *background* under `--al-ink` text, 9.99:1), `--al-light` #9ca3af = **2.54:1** (fails), `#94a3b8` (27 uses) = 2.56:1, `#38bdf8` (21 uses) = 2.14:1.
  - **Verified**: typecheck, production build (54/54 routes), and — since this is a visual fix — real-browser verification at 1440px and 390px confirming the Peserta card now renders its blue gradient with a visible white icon, heading, readable body copy and visible CTA link, the other two cards keep their light treatment, the hero subline computes to `rgb(30, 90, 143)` with no gradient, and zero console/page errors.
  - **Deliberately still open (queued, verified but out of this cycle's scope)**: the two hero floating cards physically **overlap by 123×82px** at 1440px while the hero image renders at only 261×274px and the 450px marquee overhangs it; the `<mark>` color-crop on the headline breaks into ragged mismatched blocks across lines; the half-applied "Field Dossier" seam (hero in Fraunces, the very next section in Inter); ScrollStack cards that are 855×300px slabs holding ~15 words. A 9-lens product-wide design audit (surfaces + CSS architecture + a11y + responsive + motion, with adversarial verification and a Creative Director synthesis) is running to turn these into a sequenced roadmap — token consolidation first, since polishing sections on top of 1,492 `!important` reproduces bugs like this one indefinitely.
- **2026-07-26 [Phase 29 - FAQ "Field Notes" Redesign]**: Twentieth autonomous cycle, continuing "The Field Dossier" visual-redesign direction into the landing page FAQ section. Added a numbered index (`01`–`05`, Space Grotesk, tabular-nums, royal-blue) to the left of each question, matching the exhibit/ledger numbering motif used elsewhere; question text set in Fraunces italic (`.al-faq-question`); the section heading also switched to Fraunces to match. Deliberately kept the existing native `<details>`/`<summary>` open/close mechanism and chevron-rotation animation as-is (both already work correctly) rather than attempting the fragile CSS `grid-template-rows` smooth-collapse trick native `<details>` needs for an animated open/close — that technique has real cross-browser risk (native UA `display:none` toggling doesn't animate) and wasn't worth the added risk for this cycle; noted as a deferred, lower-priority refinement. Verified with typecheck, changed-file lint, a full production build (54/54 routes), and — since this is a visual change — loaded the page with Playwright, precisely scrolled to the FAQ section (`getBoundingClientRect` instead of relying on anchor-scroll, which landed on the wrong section once), and screenshotted both the closed list and an opened item to confirm the typography and numbering actually render as intended (caught and fixed one real bug this way: the first screenshot showed the index/question still in plain Inter because the corresponding CSS classes hadn't been added yet — code review alone would have missed this).
- **2026-07-26 [Phase 28 - Vercel Deploy Failure Investigation & Cleanup]**: Nineteenth autonomous cycle. The project owner asked to actively investigate and fix Vercel's deployment failures (a scope change from the earlier "ignore Vercel, focus on VPS migration" instruction — merge decisions still ignore Netlify/Vercel checks, but Vercel's actual build health is now back in scope). This sandbox has no Vercel dashboard/API/CLI access (no auth token; `npx vercel login`/`whoami` can't even reach Vercel's servers from here), so root-caused via static analysis instead of live logs.
  - **Most likely root cause (cannot fix from code, needs the project owner to check the Vercel dashboard)**: `prisma/schema.prisma` requires both `DATABASE_URL` and `DIRECT_URL` (Prisma resolves `env()` refs at schema-parse time, so `prisma generate` — the first step of `vercel.json`'s buildCommand — fails immediately if either is entirely unset), and `services/auth.ts` throws unconditionally at **module import time** (not lazily per-request) if `JWT_SECRET` is unset while `NODE_ENV === "production"` — which `next build` always sets internally. ~50 route/page files import that module, so `next build`'s page-data-collection phase would trip this on any page. Both failure modes produce the exact symptom observed (fails on every PR/Preview deploy, passes in GitHub Actions CI and local builds — the only two places currently injecting dummy values for these vars). `vercel.json` has no `env` block and Vercel never reads the gitignored `.env`/`.env.local`, so the only source of these values is Vercel's own dashboard. **Action needed from the owner**: in Vercel → Project Settings → Environment Variables, confirm `DATABASE_URL`, `DIRECT_URL`, and `JWT_SECRET` exist and are scoped to include **Preview** (not only Production) — a Production-only scoping would explain "every PR fails" while a hypothetical push-to-main deploy might still succeed. If already set correctly, the actual Vercel build log's first error line would confirm or rule this out immediately (a Prisma "`Environment variable not found`" message vs. a stack trace pointing at `services/auth.ts`) — pasting that log is the fastest way to close this out.
  - **Real bugs found and fixed (safe, code-only, verified with a full build)**: (1) `prisma/init.ts` was a dead legacy script that manually created a raw-SQL SQLite schema (`Institution`/`User`/`Module`/`Lesson` tables) totally divergent from the current Postgres `prisma/schema.prisma` — it predates the project's migration to Postgres/Supabase and would produce a broken DB if it were ever actually run again; deleted it and fixed the `"setup"` npm script (which called it) to use the same `prisma db push --accept-data-loss && prisma db seed` flow `"build:local"` already uses correctly. (2) Removed the now-unused `better-sqlite3`/`@types/better-sqlite3` devDependencies (only consumer was the deleted script) — installing a native addon on every `npm install` for a dependency the current architecture doesn't use is unnecessary build risk. (3) `next.config.ts` unconditionally set `output: "standalone"`, which only the Docker/VPS deploy path consumes (`Dockerfile`/`ecosystem.config.js` run `.next/standalone/server.js`) — Vercel has its own independent serverless bundling and never reads this output, so it was wasted build work (and prints a confusing "not needed on Vercel" warning); made it conditional on `!process.env.VERCEL` (a Vercel-provided system env var), verified both branches actually build correctly (with and without `VERCEL=1` locally). (4) Added `"engines": {"node": "20.x"}` to `package.json` to match the Node version GitHub Actions CI already pins, removing Vercel's Node-runtime selection as a possible source of drift between environments. (5) `.env.example` still documented a since-removed `ANTHROPIC_API_KEY` under a "3b. ASISTEN AI CLAUDE" heading — the AI tutor was migrated to Phi-3/OpenAI-SDK in an earlier phase (see commit `7177b50`) and actually reads `PHI3_API_KEY`/`PHI3_BASE_URL`/`PHI3_MODEL` (or the `OPENAI_*` equivalents), which were entirely undocumented; updated the file to match reality.
  - **Deliberately left alone**: `NEXTAUTH_SECRET`/`NEXTAUTH_URL` are still validated by `scripts/validate-env.mjs` (used by the VPS deploy workflow) despite the app having no `next-auth` dependency and never reading them at runtime — fixing that touches deployment/CI config, which is out of scope for a code-only Vercel investigation.
- **2026-07-26 [Phase 27 - Metrics Bar Ledger Redesign]**: Eighteenth autonomous cycle, continuing "The Field Dossier" visual-redesign direction into the landing page's metrics bar (per Phase 26's deferred follow-up list). Replaced the 4-equal-card grid (white cards, borders, shadow, static numbers) with a single ledger row (hairline top/bottom rule, no card chrome), each stat's number set in Fraunces at up to 4rem with `font-variant-numeric: tabular-nums`, caption in small-caps Space Grotesk underneath. Added a real count-up animation: extracted `components/ui/MetricsLedger.tsx` (client component) using `IntersectionObserver` to trigger a single ~800ms eased rAF count-up per stat the first time it scrolls into view (one-shot, respects `prefers-reduced-motion` and the no-`IntersectionObserver` fallback by rendering the final value immediately) — no scroll listener, reusing the same one-shot-observer pattern already established by the sitewide reveal engine rather than inventing a new scroll-driven mechanism. Changed the `metrics` data in `app/page.tsx` from pre-formatted strings (`"2.500+"`) to `{value, suffix, label}` objects so the component has a real number to animate from 0. Verified with typecheck, changed-file lint, a full production build (54/54 routes), and — since this is a visual/animation change — actually loaded the page with Playwright, scrolled the metrics bar into view, and screenshotted both a mid-animation frame and the settled final state to confirm the count-up genuinely runs (2,144→2,500 etc.) rather than assuming it from code review.
- **2026-07-26 [Phase 26 - Landing Hero Redesign: "The Field Dossier"]**: Seventeenth autonomous cycle, first implementation cycle of the visual-redesign initiative the project owner requested (modern, "Gen Z," premium, non-generic-AI-SaaS aesthetic for the landing page and LMS shell). Used a judge-panel workflow (3 independent art-direction pitches — editorial, kinetic, structural — scored and synthesized) to pick one coherent creative direction rather than a single unreviewed guess, then implemented it for the hero section only (subsequent sections queued for future cycles).
  - **Direction**: "The Field Dossier" — an editorial/case-file metaphor (numbered exhibits, ledger-style stats, pull-quote cards) instead of the generic centered-hero-plus-icon-cards template. Added **Fraunces** (display, via `next/font/google`) and **Space Grotesk** (labels/eyebrows/numerals) in `app/layout.tsx` alongside the existing Inter, wired only into the hero this cycle.
  - **Hero changes** (`app/page.tsx` + `styles/akseslegal-theme.css`): replaced the rounded badge pill with a flush-left "Exhibit 00 · Leadership Dossier" eyebrow; headline set in Fraunces at a much larger scale with one phrase given a duotone royal-blue color-crop (`<mark>`) instead of a plain gradient span; tightened the headline→description spacing while widening eyebrow→headline spacing (asymmetric rhythm instead of even gaps); trust badges restyled from a dashed-border icon row into a hairline-divided, small-caps inline list; the two floating hero cards restyled from generic glass cards into a Fraunces ledger-stat card (progress %) and a giant-quote-mark pull-quote card, both with a diagonal clipped corner instead of rounded corners; added a rotated "Exhibit 01" tag overlapping the hero photo; marquee chips simplified to plain text separated by em-dashes; added a signature "ink-bleed" CTA micro-interaction (gold-to-blue `background-position` wipe on hover, layered on the existing shine-sweep/lift effect), scoped to the hero CTA only via `.al-hero-actions .al-btn-primary` rather than the shared `.al-btn-primary` base class used elsewhere.
  - **Real bug found and fixed along the way**: `styles/premium.css` has a global `h1, h2, h3, h4, h5, h6 { font-family: inherit !important; }` rule that was silently neutralizing the *existing* `.al-page h1 { font-family: var(--font-heading,...) }` mechanism sitewide (invisible before since `--font-heading` resolved to Inter anyway, the same as the inherited fallback) — had to add `!important` with higher specificity to `.al-hero-title` specifically to actually apply Fraunces; left the sitewide `premium.css` rule untouched since fixing it globally is out of scope for a hero-only cycle and needs broader visual regression testing.
  - **Verification**: typecheck, changed-file lint, full production build (54/54 routes) — and, since this is a visual change, actually started the dev server and used the pre-installed Playwright Chromium to screenshot the live hero at desktop and mobile viewports, inspect computed styles to confirm Fraunces was truly applying (not just assumed from the CSS), and hover-test the new CTA interaction — rather than relying on code review alone.
  - **Deferred to a follow-up cycle** (scoped out to keep this change reviewable): moving the avatar/rating proof row into the visual column as a photo-caption strip, per the original design brief — skipped because the existing mobile media queries assume it stays in the copy column, and moving it needs its own responsive verification pass. Also still pending: extending "The Field Dossier" direction to the metrics bar (ledger row + odometer count-up), method/pathway cards, and the FAQ ("Field Notes"), per the same judge-panel brief.
- **2026-07-26 [Phase 25 - Fix Laggy Scroll: Duplicate Lenis Instances & Unbounded Reflow Queries]**: Sixteenth autonomous cycle, prompted directly by the project owner reporting the site "feels laggy" while scrolling. Investigated by reading the three scroll-driven components in the codebase (`GlobalLeafStagger.tsx`, `LandingScrollDirector.tsx`, `ScrollStack.tsx`) and found two real, distinct bugs:
  1. **Root cause (landing page): two competing Lenis smooth-scroll instances.** `app/page.tsx` renders `<ScrollStack useWindowScroll>` twice (methodology cards, pathway cards). Each mounted instance created its **own independent Lenis instance** with its own `requestAnimationFrame` loop, both simultaneously taking over the page's native window scroll to apply smooth momentum — two engines fighting for control of the same scroll on every frame. Fixed by introducing a module-level singleton (`acquireWindowLenis`/`releaseWindowLenis`, ref-counted) so every `useWindowScroll` `ScrollStack` on a page shares one Lenis instance and one rAF loop instead of spinning up its own.
  2. **Sitewide (landing + dashboard): unbounded layout-forcing DOM queries on scroll.** `GlobalLeafStagger.tsx` (the global reveal-on-scroll animation engine active on every page) ran a scroll-triggered safety-net check that called `getBoundingClientRect()` on **every** `.leaf-item` in the whole document on every scroll frame — including elements that had already finished animating and would never need it again. Fixed by excluding already-`done` elements directly from the query (`:not([data-leaf-state="done"])`) instead of querying everything and skipping them after the fact, so a scroll on a long, mostly-settled page no longer forces a layout reflow for every already-revealed node.
  - Verified with typecheck, changed-file lint, and a full production build (54/54 routes). Additionally started the dev server and used Playwright (pre-installed Chromium) to load the live landing page and simulate a real wheel-scroll gesture end-to-end — confirmed zero console/page errors and correct, smooth scroll progression (`scrollY` advancing normally through the shared Lenis instance), rather than relying on code review alone.
  - This is the first cycle of a larger initiative the owner has now requested: continuing performance work plus a broader visual redesign (modern, "Gen Z" aesthetic — cleaner layouts, typography, animation, and interactivity) for both the landing page and the LMS shell, to be delivered incrementally across upcoming cycles rather than as one large PR.
- **2026-07-26 [Phase 24 - CoursePlayer Sidebar Memoization & Chevron Animation]**: Fifteenth autonomous cycle, continuing the ranked backlog. `components/shared/CoursePlayer.tsx`'s module-tree sidebar (`renderTree`/`moduleSummary`/`getModuleLessons`) rebuilt the entire lesson tree — every module's completion summary and every lesson button, including its `onClick` closures — from scratch on *every* component re-render, including ones with nothing to do with the sidebar (typing in the personal notes textarea, typing in the discussion box, switching tabs). Moved all three functions inside a `useMemo(() => {...}, [course.nodes, done, current?.id])` so the tree is only rebuilt when the module structure, completion state, or active lesson actually changes — fixing both a real performance issue (recompute + full sidebar DOM diff on every keystroke) and, incidentally, exhaustive-deps hygiene (defining the helpers inside the memo callback means they aren't stale closures needing separate dependency tracking). Paired with a small animation fix in the same component/file: the folder-accordion chevron (`ChevronDown`) never rotated on expand/collapse, unlike the identical accordion pattern already used on the landing page FAQ (`styles/home-landing.css` `.al-faq-chevron`) — added a matching `.player-tree-chevron` rotate-on-`[open]` rule to `styles/design-system.css`, using the existing `--teal` (#2a6ba7) royal-blue variable, no new colors. Verified with typecheck, changed-file lint, and a full production build (54/54 routes, no bundle size regressions).
- **2026-07-26 [Phase 23 - CommandPalette Accessibility & Tap-Target Fix]**: Fourteenth autonomous cycle, continuing the ranked performance/UX backlog. `components/ui/CommandPalette.tsx` (the Ctrl+K command palette) had two real UX gaps in the same file, fixed together: (1) the close button was icon-only with no padding (~20×20px), under the ~40px mobile tap-target minimum, and had no accessible name — added 8px padding (with a matching negative margin so the visual size/position is unchanged) and `aria-label="Tutup"`; (2) the whole overlay/input/results list carried zero `role`/`aria-*` semantics despite already having full working keyboard navigation in code, so screen reader users had no way to know this was a modal search or which result was highlighted — added `role="dialog" aria-modal="true"` on the overlay, `role="combobox" aria-expanded/aria-controls/aria-activedescendant/aria-autocomplete` on the input, and `role="listbox"` / `role="option" aria-selected` on the results list. No visual, schema, or logic change — purely additive accessibility attributes plus the tap-target padding. Verified with typecheck, changed-file lint, and a full production build (54/54 routes, no bundle size regressions).
- **2026-07-26 [Phase 22 - Memoize CourseCard, Remove Dead Preconnect]**: Thirteenth autonomous cycle, continuing the ranked performance/UX backlog from Phase 21. Two mechanical fixes: (1) `components/ui/CourseCard.tsx` was re-rendered for every course in the catalog grid on any unrelated parent state change (e.g. toggling the mobile filter panel in `ProgramCatalog.tsx`), since it wasn't memoized — confirmed `ProgramCatalog.tsx` already wraps the filtered course list in `useMemo`, so `course` object references stay stable across such re-renders and `React.memo` is actually effective here (not a no-op). Wrapped the component export in `memo()`. (2) `app/layout.tsx` had a `<link rel="preconnect">`/`<link rel="dns-prefetch">` pointing at the Supabase Postgres connection-pooler hostname — confirmed via full-repo grep that this host is never fetched by the browser (Prisma only talks to it server-side), so the hint was pure dead weight competing with real render-critical resources for the browser's early-connection budget. Removed both lines. No visual, schema, or logic change either way. Verified with typecheck, changed-file lint, and a full production build (54/54 routes, no bundle size regressions).
- **2026-07-26 [Phase 21 - Fix Oversized Dashboard/Mentor Thumbnail Image Requests]**: Twelfth autonomous cycle, first under a further-broadened directive: the 24/7 loop now also covers performance, loading time, and UX/responsiveness, not just design/animation — still strictly within the existing royal-blue palette for anything visual. Sourced from a parallel 5-lens performance/UX audit (images/media, fonts/loading, bundle/code-splitting, React render performance, UX/responsiveness) plus a judge ranking. Top pick: three `<Image fill>` thumbnails had no `sizes` prop — `app/dashboard/page.tsx:245` (student's enrolled-course list, `.dash-enroll-thumb`), `app/dashboard/page.tsx:378` (mentor's own-course list, `.dash-mentor-course-thumb`), and `app/mentor/page.tsx:80` (mentor overview list) — so Next.js defaulted every one of them to `sizes="100vw"` and fetched a full-viewport-width image for a tile rendering at 62-85px, on the two highest-traffic authenticated pages. Verified actual rendered size via CSS (`styles/enterprise-lms.css:701-705`: both dashboard thumb classes cap at 74px desktop / 62px mobile; `app/mentor/page.tsx` uses an inline `w-[85px] h-[60px]` container) before picking exact values — added `sizes="80px"` to both dashboard thumbnails and `sizes="85px"` to the mentor list thumbnail, cutting the transferred image bytes on both pages without any visual change. No schema change, no logic change. Verified with typecheck, changed-file lint, and a full production build (54/54 routes).
  - **Backlog (ranked, ready for upcoming cycles, from the same audit)**: `React.memo` on `components/ui/CourseCard.tsx` (catalog grid re-renders every card on unrelated parent state); dead `preconnect`/`dns-prefetch` to the Supabase pooler host in `app/layout.tsx:81-82` (never fetched by the browser, wastes an early-connection slot); `CommandPalette.tsx` close-button tap target too small + no `aria-label`; `CoursePlayer.tsx` module-tree sidebar (`renderTree`/`moduleSummary`) rebuilding on every unrelated state change (e.g. typing in notes) — needs `useMemo`; `BuilderClient` (730-line course builder) statically imported instead of `next/dynamic`; `CommandPalette.tsx` missing `role`/`aria-*` semantics for screen readers. Also still queued from the prior design/animation audit (Phase 20): landing metrics-bar/activity-frame hover, dashboard header-toolbar/quick-item transitions, course-card CTA button, CoursePlayer chevron rotation, forum reply-card hover-lift, `pro-lms.css:574` shimmer keyframe-name typo bug, dead `ring-fill` keyframes.
- **2026-07-26 [Phase 20 - Fix Off-Brand Mint Quality Badge]**: Eleventh autonomous cycle, second under the design/animation-focus directive. Used a parallel multi-agent audit (5 independent reviewers covering landing page, dashboard, course catalog, course player/forum, and global CSS consistency) followed by a judge synthesis to rank candidates by visual impact vs. implementation risk. Winner: `.course-quality-badge` ("Pilihan Eksekutif" sparkle badge, rendered on every top-rated `CourseCard` across catalog/dashboard/program pages — the single most-reused card component in the app) in `styles/enterprise-lms.css` had hardcoded `background: #eefcf8` / `border: 1px solid #ccefe9` — a stray mint/cyan-green hex literal, the same off-brand hue family purged as rgba values in the prior cycle, just missed because these were hex, not rgba. Replaced with `rgba(42, 107, 167, 0.08)` background / `rgba(42, 107, 167, 0.22)` border, matching the royal-blue glass-wash convention used elsewhere; left the adjacent `.course-quality-badge.master` amber/gold variant untouched since it's an intentional distinct badge, not off-brand teal. Verified with typecheck and a full production build (54/54 routes, CSS-only). Other audited-but-not-yet-implemented design/animation candidates (metrics-bar/activity-frame hover on landing, header-toolbar/quick-item transition gaps on dashboard, course-card CTA button, course-player accordion chevron rotation, forum reply-card hover-lift, a `shimmer-swipe`/`shimmer-sweep` keyframe-name typo bug in `styles/pro-lms.css:574` that silently disables the dashboard trophy shimmer, and unused `@keyframes ring-fill`) are queued for upcoming cycles — see the workflow run for the full list.
- **2026-07-26 [Phase 19 - Fix Off-Brand Teal Hover/Focus Glow]**: Tenth autonomous cycle, and the first under the project owner's new directive to shift the 24/7 loop toward design/animation polish (instead of data/functionality fixes), while staying strictly within the existing royal-blue (#2A6BA7) glassmorphism color palette. Found a genuine leftover-rebrand bug: `styles/enterprise-lms.css` already defines `--enterprise-teal: #2a6ba7` (the current brand blue) at `:root`, but ~19 hardcoded `rgba(15, 147, 135, …)` literals elsewhere in the same file (plus 7 more in `styles/taste.css`) never got converted to the variable and still render the old teal-green (#0F9387) — visible as an off-brand green glow on hover/focus for every stat card, course card, dashboard panel, the global keyboard-focus ring, page background wash, and text-selection color, diverging visibly from the landing page's consistent royal-blue treatment (`styles/design-system.css`/`styles/akseslegal-theme.css`). Also converted the matching dark-teal pairing `rgba(11, 111, 102, …)` (used in the student dashboard hero banner gradient) to `rgba(30, 90, 143, …)`, which is exactly `--enterprise-teal-dark: #1e5a8f` — so the fix is a mechanical, same-palette color correction, not a new design decision. Deliberately left untouched: the mentor hero banner's distinct blue/teal duo-gradient and the loading-skeleton's blue/teal duo-wash, since those use different accent values (not the old-teal literal) and read as intentional multi-hue treatments rather than the same leftover-rebrand bug. Verified with typecheck and a full production build (54/54 routes, no bundle size changes since it's CSS-only).
  - **Backlog (functionality gap, not design — flagged by this cycle's discovery agent, deferred per the new design/animation focus)**: `app/kalender/page.tsx`'s mentor "Tambah Jadwal" button has no `onClick`/form at all — completely dead — even though a full working CRUD API already exists at `app/api/calendar/route.ts`. Same class of bug as the Phase 15 forum-reply-form fix. Also noted: `app/dashboard/page.tsx`'s admin report "Nilai Rata-rata" is derived from `progressPercent * 0.9` instead of real `AssessmentAttempt.score`; the analytics page's "Weekly Activity Trend" chart is a hardcoded static array mislabeled "Real-time" despite real `ActivityLog` data being queried on the same page; and the transcript PDF export uses hardcoded `totalXP`/`badgesCount`/`attendanceRatePercent` instead of real `XPLog`/`UserBadge`/`AttendanceRecord` data. All four are real, schema-free-fixable gaps — revisit once the design/animation focus period ends or if the owner asks for a functionality cycle.
- **2026-07-26 [Phase 18 - Fix Certificate Signature Mismatch]**: Ninth autonomous feature cycle. `app/sertifikat/[number]/page.tsx` hardcoded the italic "handwritten signature" line to a literal `"Ratna Maharani"` for every certificate, while the bold printed name directly below it correctly used `cert.course?.mentor?.name`. For any course taught by a mentor other than Ratna Maharani, the signature and the printed name visibly disagreed on the same certificate. Fixed by using `cert.course?.mentor?.name` (with the same existing fallback) for the signature too, so both lines always match the certificate's actual mentor. No schema change. Verified with typecheck, changed-file lint, and a full production build (54/54 routes).
- **2026-07-26 [Phase 17 - Real Last-Active Status for Students]**: Eighth autonomous feature cycle. Found another hardcoded-fake-data case in `app/dashboard/peserta/page.tsx` (mentor's student roster): every single student row showed a fixed literal `"Aktif"` in the "Status" column, regardless of whether that student had touched the platform recently, a month ago, or ever — a mentor had no real way to tell an engaged student from a dormant one. Fixed by batch-fetching each student's most recent `ActivityLog` row (`distinct: ["userId"]`, ordered desc — same pattern used for the Phase 13 login streak and Phase 11 forum notifications) and rendering a real relative-time label via a new `formatRelativeTime()` helper in `utils/index.ts` ("Baru saja" / "X menit lalu" / "X jam lalu" / "X hari lalu" / "X bulan lalu" / "Belum pernah aktif" for students with zero logged activity). No schema change — `ActivityLog` already existed and is already populated on every login/enroll/lesson-completion/etc. Also clarified for the project owner (again, explicitly) that the recurring Netlify/Vercel webhook noise on every PR comes from GitHub App integrations at the platform level, not from any code or hosting-related work — confirmed the repo has zero remaining functional Netlify config (only a harmless `.netlify/**` ignore pattern), and that stopping the noise entirely requires the owner to uninstall the Netlify GitHub App or delete the Netlify sites, which is outside what a code change can do. Verified with typecheck, changed-file lint, and a full production build (54/54 routes).
- **2026-07-26 [Phase 16 - Real Pending-Grade Count on Mentor Dashboard]**: Seventh autonomous feature cycle. Another hardcoded-fake-data case: the mentor dashboard's "Tugas Menunggu" (assignments awaiting grading) stat card was a literal `value={0}`, always showing zero regardless of how many essays/assignments were actually sitting in `PENDING_GRADE`. Fixed by adding `prisma.assessmentAttempt.count({ where: { assessment: { course: { mentorId: user.id } }, status: "PENDING_GRADE" } })` alongside the existing mentor courses query in `app/dashboard/page.tsx`, mirroring the exact filter already used by `/mentor/evaluasi`. No schema change — `AssessmentAttempt.status` already existed. Verified with typecheck, changed-file lint, and a full production build (54/54 routes).
- **2026-07-26 [Phase 15 - Fix Non-Functional Forum Reply Form]**: Sixth autonomous feature cycle. Found a genuinely broken core feature rather than a fake-data cosmetic issue: the "Tulis Balasan" reply form on `app/forum/[threadId]/page.tsx` was static server-rendered markup — `<button type="button">` with no handler and no form submission wiring at all. Users could not post a forum reply from the thread page under any circumstance, even though the backend (`POST /api/forum/reply`, built in Phase 11) was fully functional, validated, and notification-wired. Added `components/shared/ForumReplyForm.tsx`, a small client component that posts to the existing endpoint, surfaces validation/rate-limit errors inline, and calls `router.refresh()` on success so the new reply and notification take effect immediately — no schema or backend changes needed, only wiring the UI to what already existed. Verified with typecheck, changed-file lint, and a full production build (54/54 routes). Also clarified for the project owner that ongoing Vercel/Netlify CI noise on every PR comes from platform-level GitHub App integrations unrelated to feature work — no hosting/deployment code has been touched since the VPS-migration-focus instruction.
- **2026-07-26 [Phase 14 - Real Course Search in Command Palette]**: Fifth autonomous feature cycle. Another fake-data case: the Ctrl+K command palette's "Materi" search category was 3 fully hardcoded entries, two of which pointed at the exact same single course slug (`fondasi-kepemimpinan-berdampak`) regardless of what courses actually exist. Searching for any other real program's name returned nothing. Replaced with a real fetch to the existing public `GET /api/courses` endpoint (already used by the catalog page — cached, rate-limited, no new backend needed), populating the "Materi" category with every actually-published course, linking to `/program/[slug]` so it works regardless of enrollment status. Fetches once per palette open, not on every keystroke. Verified with typecheck, lint, `dup:check`, and a full production build (54/54 routes, no bundle size regressions).
- **2026-07-26 [Phase 13 - Real Login Streak]**: Fourth autonomous feature cycle. Found an even more blatant fake-data case than Phase 12: the dashboard header unconditionally showed "7 hari beruntun" (7-day streak) for every single user, hardcoded, regardless of any real activity — a first-time login would falsely claim a week-long streak. Added `services/streak.ts` (`getLoginStreak`), which computes a real consecutive-day streak from `ActivityLog` `USER_LOGIN` entries (already logged on every local + Google OAuth login, just never used for this). `DashboardChrome` is now an async server component that fetches the real streak and passes it to `DashboardChromeClient`; the badge only renders when the streak is actually ≥ 1 day, instead of always showing a fake number. Verified with typecheck, lint, `dup:check`, and a full production build (54/54 routes, no bundle size regressions).
  - **Backlog (needs schema work, deferred per current DB-work pause)**: `app/program/[slug]/page.tsx` also displays `course.rating` (a star rating) next to a hardcoded literal `"(128 ulasan)"` — that review count is not computed from anything, it's a fixed string in the JSX for every course. There is no review/rating model in `prisma/schema.prisma` at all, so a real fix needs a new table (e.g. `CourseReview`) to actually collect and aggregate ratings — out of scope while database changes are paused. Flagging here so it isn't lost.
- **2026-07-26 [Phase 12 - Fix Fake Student Count]**: Third autonomous feature cycle. Found a real data-integrity bug rather than another notification gap: `Course.studentsCount` — the "X peserta" social-proof number shown on every program detail/catalog/dashboard card — was only ever set from `prisma/seed.ts` (hardcoded demo values like 1248, 874, 516) and was never incremented when a real user enrolled via `POST /api/progress` (`action: "enroll"`). Every course's displayed enrollment count has been permanently stale/fake since the first real signup, which directly conflicts with `soul.md`'s "Hindari data atau kemampuan palsu" principle. Fixed by incrementing `studentsCount` inside the existing enroll transaction, only on genuinely new enrollments (not re-enroll no-ops). No unenroll feature exists anywhere in the codebase, so no decrement path was needed for symmetry. Note: this stops the count from drifting further but doesn't retroactively correct courses that already accumulated real enrollments under the old code — that would need a one-time backfill (`studentsCount = COUNT(enrollments)` per course) run directly against the production database, which is out of scope for an autonomous code change. Verified with typecheck, lint, `dup:check`, and a full production build (54/54 routes).
- **2026-07-26 [Phase 11 - Forum Reply Notifications]**: Second autonomous feature cycle. Same notification-gap pattern as Phase 10, found in the discussion forum this time: `POST /api/forum/reply` created the reply and bumped the thread's `updatedAt`, but never notified anyone — a thread's original author and any other participants had no way to know someone replied except by manually revisiting the thread. Added a `Notification` (type `FORUM_REPLY`) for the thread author plus every distinct prior replier (excluding whoever just replied), linking to `/forum/[threadId]`. Also switched the recurring feature-development routine's merge policy to fully autonomous: once a cycle's PR passes the real quality gates (typecheck/lint/dup-check/build/smoke via GitHub Actions), it merges itself — Netlify/Vercel checks are ignored for merge decisions per the project owner's explicit instruction to deprioritize deployment while a VPS migration is pending. Verified with typecheck, lint, `dup:check`, and a full production build (54/54 routes).
- **2026-07-26 [Phase 10 - Assessment Grading Notifications]**: Started an ongoing autonomous feature-development cycle (recurring every 6 hours) to keep expanding the LMS while deployment/database work is paused. First gap found: when a mentor grades an essay/assignment (`POST /api/mentor/evaluations/[attemptId]/grade`), the student was never notified — the `Notification` model and bell UI already exist and are used for attendance/broadcast/material events, but grading completion didn't create one, so a student had no way to know their async-graded work was reviewed except by manually revisiting the course. Added a `Notification` (type `ASSESSMENT_GRADED`) inside the existing grading transaction, worded differently for pass/fail, linking to `/riwayat`. Also found `/riwayat` (the student learning-history timeline) showed evaluation entries as generically "Selesai" even while an essay was still `PENDING_GRADE` awaiting the mentor, never linked back to the course, and never surfaced the mentor's written feedback at all despite it being captured server-side. Fixed all three: a distinct "Menunggu Penilaian" state, a link to the course, and the feedback text rendered as a quoted note. Verified with typecheck, lint, `dup:check`, and a full production build (54/54 routes, `/riwayat` bundle size unchanged).
- **2026-07-26 [Phase 9 - Drop Netlify, VPS Deploy Prep]**: Removed Netlify as a deployment target — its PR preview checks were failing near-instantly (1–4s, too fast for a real build) with no accessible logs, which pointed to a broken/unconfigured integration rather than a code issue. Deleted `netlify.toml` and `docs/DEPLOYMENT_NETLIFY.md`, and updated the two docs/UI copy references (`docs/governance/ROPA-PRIVACY-RECORD.md`, `components/shared/MentorCourseActions.tsx`) that mentioned it. Note: the Netlify GitHub App/site connection itself still needs to be disconnected from Netlify's dashboard or the repo's GitHub App settings — that's outside what a code change can do. Also found and fixed real CI duplication: `deploy/ci/production.workflow.yml` and `deploy/vps/vps-deploy.workflow.yml` were two near-identical "quality gate + SSH deploy to VPS" workflows; consolidated into the single, more defensive `vps-deploy.workflow.yml` (explicit `StrictHostKeyChecking`, pre-flight secret validation, no third-party SSH action, release-SHA verification on the target host) and deleted the redundant one. Verified all VPS deploy tooling (`Dockerfile`, `docker-compose.yml`, `deploy/vps/*.sh`, `ecosystem.config.js`, nginx configs, `.env.example`) has no stale paths left over from the Phase 8 restructure. Copied both workflow templates into `.github/workflows/` now that the repo token has `workflow` permission, activating the quality gate and VPS deploy pipeline. Verified with typecheck, lint, `dup:check`, and a full production build (54/54 routes). Activation immediately surfaced a real bug: the VPS workflow's job-level `NODE_ENV=production` made `npm ci` silently skip all devDependencies (npm's `--omit=dev` behavior), which broke the `prepare: husky` lifecycle script and would have broken lint/build too — this was almost certainly also failing Vercel's build the whole time. Fixed with `"prepare": "husky || true"` in `package.json` and by dropping the unneeded job-level `NODE_ENV=production` (the build/validate-env steps don't need it ambiently). Re-verified end to end; the VPS workflow's `Quality gates` job now passes every step (install, validate-env, typecheck, lint, build, perf budget, smoke).
- **2026-07-26 [Phase 8 - Project Restructure & Auto-Fix Pipeline]**: Reorganized the codebase to match a standard React layout adapted for Next.js App Router: split `lib/` into `services/` (DB/auth/rate-limit/uploads) + `services/export/` (docx/pdf/pptx/xlsx generators) + `utils/`; split `components/` into `components/ui/` (presentational) and `components/shared/` (feature/business logic); moved global CSS into `styles/`; added `hooks/`, `context/`, `types/`, `constants/`. Deleted two dead/unimported CSS files (`landing.css`, `home-fresh.css`) and moved four unreferenced one-off patch scripts into `scripts/legacy/`. Eliminated real duplicate code: extracted a shared `ExportActionButton` (removed ~90% duplicated state-machine/markup across the four `Export*Button` components), extracted a `useLocalBackup` hook (removed an identical localStorage auto-backup/restore pattern duplicated between the course builder and quiz builder), and centralized the duplicated `NodeType` type into `types/course.ts`. Added tooling so this stays clean going forward: `jscpd` duplicate-code gate (`npm run dup:check`, threshold 3%, current baseline 1.96%), a husky + lint-staged pre-commit hook that runs `eslint --fix` on staged files, and a CI quality-gate template (`deploy/ci/quality.workflow.yml`, follows the existing template convention — copy into `.github/workflows/` once the repo token has `workflow` permission) that runs typecheck → autofix-and-commit → lint → duplicate-code check on every PR, blocking merge on real errors without ever changing logic automatically. Verified with a full `npm ci`, `prisma generate`, typecheck, lint, `dup:check`, and production `next build` (all 54 routes) after the restructure.
- **2026-07-13 [Phase 7 - Production Resilience]**: Hardened `getCurrentUser()` so transient Prisma/Supabase connection failures fall back to guest rendering instead of taking public pages into the global error boundary. Verified with a deliberately unavailable database, plus typecheck, lint, build, smoke, and live production health checks.
- **2026-07-13 [Phase 6 - Credential Verification]**: Replaced simulated admin certificate checks with database-backed verification, added the public `/verifikasi` portal with loading/success/error states, linked it from the admin panel and footer, and extended the smoke test to cover the new route.
- **2026-07-13 [Phase 5 - Landing Page Refinement]**: Refined the public landing page around the active royal-blue identity (`#2A6BA7`), repaired the unstyled insight cards, added an accessible FAQ section with an organization CTA, improved CTA hierarchy and focus states, and verified with typecheck, changed-file lint, production build, and the full smoke test.
- **2026-07-05 [Phase 1 - Vercel Deployment]**: Successfully configured dynamic Google OAuth callback paths (`/api/auth/google/callback` and `/api/auth/callback/google`), implemented `/tmp` SQLite mirroring for Vercel serverless, and deployed live to `profas-leadership-lms.vercel.app`.
- **2026-07-05 [Phase 2 - Autonomous Loop Initiation]**: Completed `/grill-me` architectural interrogation. Aligned on 3 priorities: Automated Assessment & Certification Engine, Modern Glassmorphism UI, and Cloud PostgreSQL Migration Readiness. Created `implementation_plan.md` and initialized `CLAUDE.md` memory.
- **2026-07-05 [Phase 3 - Autonomous Feature Execution & QA]**: Implemented Modern Glassmorphism styling across Landing Page and Dashboard (`.glass`, `.glass-card`). Integrated Interactive Quiz Engine (`/kuis/[id]` and `/api/kuis/submit`) with timer and auto-grading. Created PostgreSQL Cloud Migration Guide (`docs/MIGRATION_POSTGRESQL.md`). Performed self-debugging to fix 28 lint/type issues; achieved 100% clean typecheck, lint, and production build (`npm run build`). Ready for Vercel auto-deploy.
- **2026-07-05 [Phase 4 - Executive Interactivity & Gamification]**: Enhanced Course Player (`components/CoursePlayer.tsx`) with interactive personal lesson notes saved locally and downloadable as `.txt`, plus glassmorphism downloadable material cards. Upgraded Leaderboard (`app/peringkat/page.tsx`) with a glowing Gold/Silver/Bronze Top 3 Podium. Refined Analytics Dashboard (`app/dashboard/analitik/page.tsx`) with glassmorphism metric cards and gradient activity distribution charts. Passed 100% typecheck, lint, and production build. Automatically deployed to Vercel via GitHub push.
